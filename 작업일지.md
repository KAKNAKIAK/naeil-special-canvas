# 작업일지 — 내일스패셜 캔버스

## 2026-07-16

- 상태: 완료
- 목표: PPT·Excel 기획안 작업을 720px 구조형 캔버스 웹앱으로 전환
- 구현: 23개 블록, 이미지 라이브러리, 레이어 순서, 속성 편집, 자동 저장, 실행 취소/다시 실행, 검수, YAML/JSON/HTML/PNG/ZIP/PDF 내보내기
- 설계 기준: `.agents/skills/naeil-special-html` v2 전달 단계·이미지 provenance 규칙
- 검증: TypeScript 통과, Vitest 4건 통과, Vite production build 통과
- 브라우저: 로드·콘솔·블록 추가·프로젝트명 편집·실행 취소 활성화·전달 단계·최종본 이미지 차단·내보내기 메뉴·390px 모바일 드로어 확인
- 실행 보정: Google Drive 가상 파일시스템의 `node_modules` 쓰기 오류를 피하기 위해 `%LOCALAPPDATA%\NaeilSpecialCanvasRuntime` 로컬 런타임을 사용
- 다음: 실사용 기획안으로 이미지·문구를 넣고 디자이너 전달 ZIP 검토

## 2026-07-21

- 상태: 포터블 EXE 패키징 완료
- 생성: `desktop/main.cjs`, `desktop/package.json`, `build-exe.bat`, `release/내일스패셜-캔버스-1.0.0-portable.exe`
- 방식: Electron 43.1.1 + electron-builder 26.15.3, Windows x64 portable target
- 실행 데이터: `%APPDATA%\NaeilSpecialCanvas`에 WebView 데이터와 IndexedDB 저장
- 빌드 보정: Vite `base: './'`을 적용해 EXE 내부 `file://` 경로에서도 CSS·JS 자산을 정상 로드
- 검증: 웹 자동 테스트 8건, Vite production build, EXE 생성, Windows 실제 실행, 초기 캔버스·자동 저장·블록·이미지·미리보기·내보내기 및 전달 ZIP/PNG/PDF/YAML/HTML 메뉴 확인
- 배포 파일: 90,931,084 bytes(86.72 MB), SHA-256 `D72922BDF6F4DAAA6C44F3EE40BE34BF7401902E47B4061814D316AFD94F0A6C`
- 남은 리스크: Authenticode 코드 서명이 없어 다른 PC에서 SmartScreen 경고가 표시될 수 있음

## 2026-07-21 빈 캔버스 시작 화면

- 변경: 첫 시작 및 새 프로젝트 생성 시 발리 예시 대신 제목·블록·이미지·특전·CTA가 없는 빈 기획안 생성
- 화면: 빈 캔버스에는 블록 추가 또는 기본 정보 입력을 안내하는 시작 화면 표시
- 검증: TypeScript, Vitest 8건, Vite production build, 포터블 EXE 재빌드 통과
- 배포 파일: 89,525,956 bytes(85.38 MB), SHA-256 `D938F8A28397F488A1B7966D1E3D9D0F8A635A4925848A2115CC22D82FE41A34`
- 패키징 보정: Electron 로컬 캐시에 잘못 연결된 프로젝트 참조를 제거해 EXE 내부에 이전 EXE가 중복 포함되지 않도록 수정

## 2026-07-21 다음 블록 추가 동작 보정

- 원인: 캔버스 하단 `다음 블록 추가` 버튼이 블록을 생성하지 않고, 이미 열린 블록 패널만 다시 여는 동작으로 연결되어 있었음.
- 변경: 버튼 클릭 시 기본 `텍스트` 블록을 즉시 추가하고 해당 블록을 선택·편집 상태로 전환하도록 수정.
- 검증: TypeScript, Vitest 9건, Vite production build, 포터블 EXE 재빌드 통과.
- 배포 파일: 89,526,585 bytes(85.38 MB), SHA-256 `734F80A7BB659FE8CE0579BD06295E1330C0122E06C186DEDF7409E7DBAAB21C`

## 2026-07-21 이미지 분할 기능

- 기능: 이미지 라이브러리에서 원본을 선택해 가로 2분할, 세로 2분할 또는 2 × 2 분할을 실행.
- 처리: 원본 이미지는 보존하며, 분할본은 별도 자산으로 추가되고 검토 대상(`candidate`)으로 기록.
- 품질: 홀수 크기 이미지도 마지막 행·열 픽셀이 누락되지 않도록 분할 좌표를 계산.
- 검증: TypeScript, Vitest 10건, Vite production build, 포터블 EXE 재빌드 통과.
- 배포 파일: 89,527,612 bytes(85.38 MB), SHA-256 `CA1730D6C903DE26CD2E387A3DB1BCB7AB7EBC21487EEBED55A57EC427D8213E`

## 2026-07-21 블록 내부 이미지 편집

- 기능: 선택한 블록의 오른쪽 `디자인` 패널에 이미지 편집 영역을 추가. 새 이미지 불러오기, 라이브러리 이미지 추가, 연결 이미지 교체, 블록에서 이미지 제거를 한곳에서 처리.
- 처리: 새로 불러온 이미지는 즉시 해당 블록에 연결하며, 교체 시에는 기존 연결 위치를 유지.
- 검증: TypeScript, Vitest 10건, Vite production build, Electron 포터블 패키징 통과.
- 배포 주의: 기존 기본 EXE가 실행 중이라 파일 교체가 잠겨, 최신본을 별도 파일명으로 생성.
- 배포 파일: `내일스패셜-캔버스-1.0.0-이미지편집-portable.exe`, 89,528,196 bytes(85.38 MB), SHA-256 `71FE3D02F60099B8A8006EEA30343EE0C023932C1C7D4517E8D723E72B6C22E8`

## 2026-07-21 블록 이미지 URL 추가

- 기능: 선택 블록의 `블록 이미지` 영역에 `https://...` 이미지 URL을 입력해 즉시 블록에 추가.
- 처리: URL 이미지에 대해 별도 출처·권리 정보 입력을 요구하지 않으며, URL이 실제 이미지로 열릴 때만 추가.
- 검증: TypeScript, Vitest 10건, Vite production build, 기본 포터블 EXE 재빌드 통과.
- 배포 파일: `내일스패셜-캔버스-1.0.0-portable.exe`, 89,528,354 bytes(85.38 MB), SHA-256 `292F0600CDC284BB7CAB992D978D808372E215C71D8A3DE89C781ACDD55E28E2`

## 2026-07-21 홀수 이미지 레이아웃 편집

- 기능: 3장·5장처럼 홀수 이미지가 연결된 블록에서 `강조 왼쪽`, `강조 오른쪽`, `균등`, `세로` 레이아웃을 선택.
- 처리: 모든 연결 이미지를 표시하고, 기존 프로젝트는 `auto` 레이아웃으로 자동 보정. `연결 이미지 정보` 패널은 화면에서 제거.
- 검증: TypeScript, Vitest 10건, Vite production build, Electron 포터블 패키징 통과.
- 배포 주의: 기본 EXE가 실행 중이라 최신본은 별도 파일명으로 생성.
- 배포 파일: `내일스패셜-캔버스-1.0.0-홀수레이아웃-portable.exe`, 89,528,803 bytes(85.38 MB), SHA-256 `6738128B3DDBF7563AD90F92077690AF0E3E1DD1884AEBCB88AC7A4B065F8DEE`
- 기본 EXE 교체: 실행 중인 기존 파일 종료 후 `내일스패셜-캔버스-1.0.0-portable.exe`도 같은 최신본으로 교체 완료.

## 2026-07-21 이미지 레이아웃 상시 편집

- 변경: 이미지가 하나 이상인 모든 블록에 레이아웃 선택기를 표시. `자동`, `1열`, `2단`, `3단`을 이미지 수와 무관하게 선택 가능.
- 추가: 이미지가 3장 이상이면 `강조 왼쪽`, `강조 오른쪽`, `균등형`도 직접 선택 가능. 자동 배치는 이미지 수에 맞춰 유지.
- 유지: `연결 이미지 정보`는 화면에 표시하지 않음.
- 검증: TypeScript, Vitest 10건, Vite production build, Electron 포터블 EXE 패키징 통과.
- 배포 파일: `내일스패셜-캔버스-1.0.0-portable.exe`, SHA-256 `FF92EF7B26C2549CCB0EBE43D442A50486C298457B458CB8FC055CD284B03099`

## 2026-07-21 사용자 정의 이미지 레이아웃

- 변경: 프리셋 외에 `직접 편집` 모드를 추가. 이미지가 1장 이상이면 사용자 정의 12칸 그리드로 전환 가능.
- 편집: 각 이미지별 가로 폭(3~12칸), 세로 높이(1~6칸), 위치(상·하·좌·우), 크롭 기준점(9방향)을 조절.
- 보존: 이미지 추가·교체·삭제 후에도 직접 배치 설정을 정리해 유지하고, 매니페스트 및 캔버스 전달 JSON에도 배치 데이터를 포함.
- 검증: TypeScript, Vitest 11건, Vite production build, Electron 포터블 EXE 패키징 통과.
- 배포 파일: `내일스패셜-캔버스-1.0.0-portable.exe`, 89,530,030 bytes, SHA-256 `B4460834313EDCAC85AFC097D769874E432C001755953A21D9D3E9B5B872EBC0`

## 2026-07-21 세로·가로 비율 추천 프리셋

- 범위: 이미지 1~2장에는 추천 프리셋을 노출하지 않고 `직접 편집`만 제공. 3장 이상일 때만 원본 비율을 읽어 추천 영역을 표시.
- 조합: 세로 1 + 가로 2는 좌·우 세로 강조, 가로 1 + 세로 2는 상·하 가로 강조, 세로 3은 세로 3단·가운데 강조, 가로 3은 상단 와이드·가로 3단, 4장 이상은 세로/가로 대표 갤러리 2종 제공.
- 처리: 추천을 누르면 사용자 정의 12칸 그리드로 적용되어 이후 위치·크기·크롭을 계속 수정할 수 있음.
- 검증: TypeScript, Vitest 12건, Vite production build, Electron 포터블 EXE 패키징 통과.
- 배포 파일: `내일스패셜-캔버스-1.0.0-portable.exe`, 89,530,913 bytes, SHA-256 `760C018A9288D1784F96122854B7C4675F3E1896D3CBFE18FDB0E7E7F7BBDB7E`

## 2026-07-21 사용자 배치 이미지 겹침 보정

- 원인: 사용자 정의 그리드가 이미지 요소 자체에 좌표를 부여해 기존 고정 이미지 높이가 그리드 행보다 클 때 다음 행까지 넘쳤음.
- 변경: 각 사용자 배치 이미지를 넘침 차단 프레임으로 감싸고, 프레임 내부에서만 `object-fit: cover` 크롭하도록 전환.
- 결과: `직접 편집`과 비율 추천 프리셋 모두 이미지가 지정 칸 밖으로 겹치지 않음.
- 검증: TypeScript, Vitest 12건, Vite production build, Electron 포터블 EXE 패키징 통과.
- 배포 파일: `내일스패셜-캔버스-1.0.0-portable.exe`, 89,531,238 bytes, SHA-256 `4D213D40B5B94A91BB639BCDE0A2F036EDA4FB5F9858C3C09EE051B623F46F00`

## 2026-07-21 수동 그리드 자동 강조 규칙 분리

- 원인: `2단`, `3단`, `균등형`, `1열`을 선택해도 자동 레이아웃의 첫 이미지 확대 규칙이 함께 적용되어 세로로 늘어나거나 빈 영역이 남았음.
- 변경: 수동 프리셋은 첫 이미지의 기존 grid span과 높이를 항상 초기화. 강조는 `강조 왼쪽`·`강조 오른쪽`에서만 발생.
- 결과: `3단`·`균등형`은 같은 높이의 3단, `2단`은 균등 2열, `1열`은 균등 세로 흐름으로 표시.
- 검증: TypeScript, Vitest 12건, Vite production build, Electron 포터블 EXE 패키징 통과.
- 배포 파일: `내일스패셜-캔버스-1.0.0-portable.exe`, 89,531,113 bytes, SHA-256 `D923AD84D4108FF2286713A725BB33DE609C67FFCD6BF8AF18F678DEB9B70C64`

## 2026-07-21 강조형 보조 이미지 행 고정

- 원인: `강조 오른쪽`에서 대표 이미지를 먼저 배치한 뒤 보조 이미지의 행이 자동 배치되어 좌측 상단 빈칸이 생겼음.
- 변경: 강조 왼쪽·오른쪽 모두 보조 이미지 2장을 각각 반대편 첫째 행과 둘째 행에 명시적으로 고정.
- 반응형: 모바일 미리보기에서는 강조형도 모든 이미지를 한 줄씩 자동 해제해 자연스럽게 세로로 표시.
- 검증: TypeScript, Vitest 12건, Vite production build, Electron 포터블 EXE 패키징 통과.
- 배포 파일: `내일스패셜-캔버스-1.0.0-portable.exe`, 89,531,132 bytes, SHA-256 `6CF1503AB7B8E6F189759978CBF82505D35162BBCE3118842EAE58A50739A247`

## 2026-07-21 블록 구조 4종 통합

- 변경: 기존 23개 이름형 블록을 `텍스트`, `이미지`, `목록`, `가격·특전` 4개 공통 구조로 통합.
- 화면: 블록 추가 패널과 블록 구조 선택 목록은 4개만 표시. 호텔 소개·부대시설·일정·FAQ 등은 제목과 내용으로 표현.
- 호환: 기존 저장 데이터와 가져온 매니페스트의 이름형 블록은 열 때 이미지·목록·가격·특전·텍스트 중 하나로 자동 변환.
- 검증: TypeScript, Vitest 13건, Vite production build, Electron 포터블 EXE 패키징 통과.
- 배포 파일: `내일스패셜-캔버스-1.0.0-portable.exe`, 89,530,963 bytes, SHA-256 `7108453330A8C71730835B67DAACAB90FB48D9CE73A63398EBAB3D3FC6AA3501`

## 2026-07-21 캔버스 이미지 드래그 위치 교환

- 기능: 캔버스 안에서 이미지를 끌어 다른 이미지 위에 놓으면 두 이미지의 위치를 교환.
- 처리: 일반 프리셋은 이미지 순서를 교환하고, 직접 편집 레이아웃은 이미지가 차지한 배치 칸을 교환해 크기·구조를 유지.
- 피드백: 드래그 중 원본은 반투명, 놓을 대상은 청록 테두리로 표시.
- 검증: TypeScript, Vitest 13건, Vite production build, Electron 포터블 EXE 패키징 통과.
- 배포 주의: 기본 EXE가 실행 중이라 교체하지 못해 최신본을 별도 파일로 생성.
- 배포 파일: `내일스패셜-캔버스-1.0.0-이미지드래그-portable.exe`, 89,531,532 bytes, SHA-256 `1F25B3AE2C0DB8F7332EFC4EB1DA244E0EA99977FB7FC77DE3E93242A05BA940`

## 2026-07-21 페이지 표지 제거

- 변경: 캔버스 상단의 청록색 페이지 표지와 장식 요소를 제거해 첫 블록부터 바로 편집하도록 변경.
- 내보내기: HTML 내보내기에서도 동일한 표지를 제거해 캔버스와 결과물이 같은 구조로 시작.
- 유지: 선택된 블록이 없을 때 오른쪽 패널의 페이지 설정에서 제목·부제·여행지를 계속 수정 가능.
- 검증: TypeScript, Vitest 13건, Vite production build, Electron 포터블 EXE 패키징 통과.
- 배포 파일: `내일스패셜-캔버스-1.0.0-portable.exe`, 89,531,094 bytes, SHA-256 `AD85A4E250EBA07ADA739A9842F977EC2C915489ED326884FF56E1C2E8A73F4B`

## 2026-07-21 레퍼런스 기반 정보표 블록 추가

- 분석 기준: `C:\Users\naeiltour0001\Desktop\이벤트모음\임시작업폴더\내일스패셜_유형_분석보고서.md` 및 특별한·골프 레퍼런스. 단순한 FAQ·체크포인트는 기존 목록으로 유지하고, 일차·동선·조건·비교처럼 열 관계가 필요한 정보만 별도 구조로 판단.
- 변경: 공통 블록을 텍스트·이미지·목록·정보표·가격·특전 5종으로 확장. `정보표`는 2열(일정·조건·비교) 또는 3열(날짜·장소·내용)을 선택하고, 줄 단위 편집으로 행을 관리한다.
- 호환/내보내기: 기존 프로젝트는 그대로 열리며, 새 `tableHeaders`·`tableRows`는 캔버스·독립 HTML·매니페스트에 함께 반영.
- 검증: TypeScript, Vitest 13건, Vite production build, Electron 포터블 EXE 패키징 통과.
- 배포 파일: `내일스패셜-캔버스-1.0.0-portable.exe`, 89,532,228 bytes, SHA-256 `0848844DB2CC910C66D07E0CFB417BB325A62E97082082D9E10964976060E2FF`

## 2026-07-21 아이콘 카드 독립 블록

- 변경: 레퍼런스의 `아이콘 + 카드 제목 + 짧은 설명` 3열 구성을 독립 `아이콘 카드` 블록으로 추가. 단순 목록과 분리해 카드별 아이콘, 제목, 설명, 청록·주황·초록 강조색을 편집한다.
- 레이아웃: 카드 2개는 2열, 3개 이상은 3열로 표시하며 360px 미리보기와 독립 HTML은 1열로 자연스럽게 전환한다.
- 호환/내보내기: `iconCards` 데이터가 프로젝트 저장, 매니페스트, 캔버스 전달 JSON, 독립 HTML에 유지된다.
- 검증: TypeScript, Vitest 14건, Vite production build, Electron 포터블 EXE 패키징 통과.
- 배포 파일: `내일스패셜-캔버스-1.0.0-portable.exe`, 89,534,330 bytes, SHA-256 `386038FD14955AA0E3061241EEAE456AB5C51E83576884DC5D8E6801BC1620AB`

## 2026-07-21 판매 데이터·배경색 제거

- 제거: 오른쪽 `판매 데이터` 탭, 캔버스 상단 가격/CTA 바, 판매 특전 자동 연동, `campaign-data.json` 단독·ZIP 내보내기, 판매 정보 기반 최종본 QA.
- 변경: `가격·특전` 블록은 블록 안에서 특전·조건을 직접 편집한다. 배경색 선택기를 제거하고 기존·불러온 블록을 모두 흰 배경으로 정규화했으며, HTML과 전달 JSON에서도 배경 변형을 내보내지 않는다.
- 검증: TypeScript, Vitest 13건, Vite production build, Electron 포터블 EXE 패키징 통과.
- 배포 주의: 기본 EXE가 실행 중이라 교체할 수 없어 별도 최신본으로 생성.
- 배포 파일: `내일스패셜-캔버스-1.0.0-판매제거-portable.exe`, 89,532,508 bytes, SHA-256 `B4AB41B18CD5D27171F2527F70D3E309BE6961FDC72CB702348B5E23BC5D83C8`

## 2026-07-21 이미지 추가 동선 통합·원본 라벨

- 변경: 파일 업로드와 URL 추가를 왼쪽 이미지 라이브러리로 통합. 캔버스에서 블록을 선택한 상태면 파일·URL 이미지가 해당 블록에 즉시 연결되고, 선택하지 않았으면 라이브러리에만 추가된다.
- 정리: 오른쪽 패널에서 이미지 업로드·URL 추가·기존 이미지 연결·교체·제거 UI를 제거하고 이미지 레이아웃 편집만 유지.
- 라벨: URL 이미지는 원본 URL, 로컬 업로드 이미지는 파일명을 이미지 카드 아래 선택 가능한 텍스트로 표시해 복사·붙여넣기로 원본을 확인할 수 있게 함.
- 검증: TypeScript, Vitest 13건, Vite production build, Electron 포터블 EXE 패키징 통과.
- 배포 파일: `내일스패셜-캔버스-1.0.0-portable.exe`, 89,533,192 bytes, SHA-256 `75A750D24503CE800650894F452696572CBABD777707316EFFC1A79A99C49657`

## 2026-07-21 블록별 작성 패널 고도화

- 문제: 모든 블록이 `작은 제목·제목·설명` 같은 동일한 MVP 입력칸을 사용해 이미지, 특전, 일정표, 아이콘 카드의 작성 목적이 흐려졌음.
- 변경: 블록별로 상단 분류·섹션 제목·안내 문장의 라벨, 예시, 작성 가이드를 달리 표시. 목록·특전은 항목 작성 영역, 정보표는 열/행 작성 영역, 아이콘 카드는 카드 편집 영역을 별도 그룹으로 구분.
- 보존: 기존 프로젝트의 텍스트 데이터 스키마와 실제 렌더 결과는 유지하며, 편집 경험과 입력 힌트만 블록 특성에 맞게 개선.
- 검증: TypeScript, Vitest 13건, Vite production build, Electron 포터블 EXE 패키징 통과.
- 배포 파일: `내일스패셜-캔버스-1.0.0-portable.exe`, 89,532,811 bytes, SHA-256 `07A09EBE6396060579478AC17514212DA640BAF1AF9170C2C8B8156EE5ECEF0A`

## 2026-07-21 텍스트·이미지 블록 배치 편집

- 변경: 블록을 텍스트 영역과 이미지 영역으로 분리하고 `텍스트 위`, `이미지 위`, `이미지 왼쪽`, `이미지 오른쪽` 배치를 선택할 수 있게 했다. 이미지 내부의 자동·분할·직접 편집 격자는 그대로 유지된다.
- 반응형: 좌우 배치는 360px 미리보기와 독립 HTML에서 한 줄로 쌓여 전체 틀이 무너지지 않는다.
- 호환/내보내기: 기존 프로젝트는 기본값 `텍스트 위`로 안전하게 읽으며, 선택한 배치는 프로젝트 저장·매니페스트·캔버스 전달 JSON·독립 HTML에 유지된다.
- 검증: TypeScript, Vitest 14건, Vite production build, Electron 포터블 EXE 패키징 통과.
- 배포 주의: 기본 패키징 임시 폴더가 잠겨 별도 출력 폴더에서 생성했다.
- 배포 파일: `내일스패셜-캔버스-1.0.0-텍스트이미지배치-portable.exe`, 89,533,342 bytes, SHA-256 `AB8CA88754C92BD185310FD226D9FC44301BFF18C4D6C0A4F5C4B0CA580B5A7A`

## 2026-07-21 블록 내부 박스 캔버스 편집

- 변경: 각 블록을 12열 스냅 그리드의 `텍스트·이미지 박스`로 편집하도록 확장했다. 캔버스 박스 상단 손잡이로 위치를 이동하고 우하단 손잡이로 크기를 조절하며, 오른쪽 패널에서 좌표·크기·앞뒤 순서를 수치로 미세 조정한다.
- 추가: 선택 블록의 `박스 배치`에서 텍스트 박스와 이미지 박스를 추가할 수 있다. 추가 텍스트는 박스별 문안을, 추가 이미지는 해당 박스별 이미지 연결을 관리하며 불필요한 추가 박스는 삭제할 수 있다.
- 호환/내보내기: 기존 텍스트·이미지 영역은 기본 박스로 자동 변환해 기존 프로젝트를 보존한다. 새 `layoutBoxes`는 프로젝트 저장, 매니페스트, 캔버스 전달 JSON, 독립 HTML에 함께 유지된다.
- 검증: Vitest 15건 통과, Vite production build 통과, 로컬 브라우저에서 박스 추가·선택·실행 취소 흐름 확인, EXE `MZ` 헤더와 SHA-256 확인. 로컬 의존성 폴더가 분리되어 TypeScript 독립 타입 검사는 실행 경로 문제로 재실행하지 못했으나 Vite 변환과 테스트는 최신 코드로 통과했다.
- 배포 파일: `내일스패셜-캔버스-1.0.0-박스캔버스-portable.exe`, 89,567,320 bytes, SHA-256 `82D4F1E4049E9658615D15BB4638C687098A452A0CF890CC6CFD6E2F7ED31110`

## 2026-07-21 PC 720px 단일 양식 고정

- 변경: 캔버스 미리보기 선택을 제거하고 전체 양식을 PC 전용 720px 하나로 고정했다. 작업 헤더와 상태바도 `720px PC 전용 양식` 기준으로 표시한다.
- 내보내기: `canvas-document.json`의 `preview_viewports`를 `[720]`으로 정리하고, 블록별 `mobile_behavior`를 제거했다. 독립 HTML도 `width=720`, `min-width:720px`, 고정 `.page{width:720px}` 구조로 내보낸다.
- 정리: `viewport-360`, `viewport-1440`, 모바일 스택용 블록 캔버스 CSS를 제거해 화면과 HTML 산출물이 같은 PC 단일 규격을 사용한다.
- 검증: 런타임 기준 TypeScript, Vitest 15건, Vite production build, Electron 포터블 EXE 패키징 통과. EXE `MZ` 헤더와 SHA-256 확인.
- 배포 파일: `내일스패셜-캔버스-1.0.0-PC720-portable.exe`, 89,535,532 bytes, SHA-256 `FB225428BC0BF72031B073CA1AE07A49A2CFA672020070AB8B3D2DFD82E5CF6E`

## 2026-07-21 레퍼런스 레이아웃 3종 블록 추가

- 추가: 첨부 레퍼런스 기준으로 `캡션 이미지 그리드`, `좌우 추천 메뉴`, `세로 타임라인` 블록을 독립 구조로 추가했다. 일반 박스 캔버스와 섞지 않고, 레퍼런스 형태가 바로 잡히는 전용 블록으로 동작한다.
- 편집: 각 블록은 오른쪽 패널에서 제목·보조문·항목을 블록 특성에 맞게 편집한다. 이미지는 왼쪽 이미지 라이브러리에 넣은 순서대로 연결되며, 캔버스에서 이미지끼리 드래그해 순서를 교환할 수 있다.
- 내보내기: 매니페스트, 캔버스 전달 JSON, 독립 HTML, ZIP 미리보기 HTML에 3종 블록 구조와 전용 CSS를 반영했다. 최종본 QA에서는 이미지형 블록으로 취급해 이미지 누락을 잡는다.
- 검증: 런타임 기준 TypeScript, Vitest 16건, Vite production build, Electron 포터블 EXE 패키징 통과. EXE `MZ` 헤더와 SHA-256 확인.
- 배포 파일: `내일스패셜-캔버스-1.0.0-PC720-레퍼런스블록-portable.exe`, 89,539,156 bytes, SHA-256 `C24A15943FF609468521DB5F92CF5C569757A7709EE4D60033993AC278C50615`

## 2026-07-22 이미지 프리셋 콘텐츠 흐름 편집

- 변경: `이미지` 프리셋을 사진 그리드 중심 구조에서 세로 콘텐츠 흐름 구조로 전환했다. 기본은 `텍스트 → 이미지`이며, 텍스트·이미지 카드를 추가해 `이미지 → 텍스트`, `텍스트 → 이미지 → 텍스트` 등으로 조립하고 위·아래 이동으로 순서를 변경한다.
- 편집: 추가 텍스트 카드는 상단 분류·제목·본문을 별도로 작성한다. 각 이미지 카드는 왼쪽 이미지 라이브러리에서 독립 연결하며, 기존 이미지 내부 레이아웃 기능은 유지한다.
- 병목 보정: 추가 이미지/텍스트 카드를 선택하면 오른쪽 패널은 선택 카드의 순서·내용·삭제만 표시하도록 축소했다. 카드 목록은 최대 높이를 제한해 카드 수가 늘어도 패널 전체를 밀어내지 않는다.
- 내보내기: 독립 HTML에서도 이미지 블록을 좌표형 캔버스가 아닌 입력 순서 그대로 흐르는 콘텐츠 섹션으로 출력한다.
- 검증: 런타임 기준 TypeScript, Vitest 17건, Vite production build, Electron 포터블 EXE 패키징 통과. EXE `MZ` 헤더와 SHA-256 확인.
- 배포 파일: `내일스패셜-캔버스-1.0.0-이미지흐름-portable.exe`, 89,562,110 bytes, SHA-256 `D2990C56B05B3C34429D117925DEC4581C3E6D83A994E290763FD831B62F641B`

## 2026-07-22 이미지 흐름 조작 위치·텍스트 입력 통일

- 정정: 오른쪽 `CONTENT FLOW` 카드 목록과 순서 조작은 가운데 캔버스에서 카드 자체를 선택할 수 있는데도 중복으로 노출되어 있었다.
- 변경: 텍스트·이미지 추가는 이미지 흐름 블록의 캔버스 하단으로, 위·아래 이동과 삭제는 선택 카드의 좌측 도구로 옮겼다. 오른쪽 패널에서는 흐름 목록과 조작 버튼을 제거했다.
- 통일: 기본 텍스트 카드와 추가 텍스트 카드가 모두 `상단 분류 · 제목 · 본문` 동일한 3개 필드와 같은 입력 형태를 사용한다. 이미지 카드를 선택하면 왼쪽 이미지 라이브러리 연결 안내만 표시한다.
- 검증: 런타임 기준 TypeScript, Vitest 17건, Vite production build, Electron 포터블 EXE 패키징 통과. EXE `MZ` 헤더와 SHA-256 확인.
- 배포 파일: `내일스패셜-캔버스-1.0.0-이미지흐름-캔버스조작-portable.exe`, 89,587,743 bytes, SHA-256 `A70AEF10CF4EA55FDC21A7B757E5B574D9DC4AAAD85B68B7D9DDF4C30D198891`

## 2026-07-22 이미지 흐름 선택 카드 레이아웃 편집 복구

- 정정: 오른쪽 `CONTENT FLOW`를 제거하는 과정에서 이미지 카드 선택 시 표시되던 이미지 내부 레이아웃 편집기까지 함께 사라진 문제를 보정했다.
- 변경: 이미지 흐름에서 이미지 카드를 선택하면 오른쪽 패널에 `자동 · 1열 · 2단 · 3단 · 직접 편집` 등 이미지 내부 레이아웃 버튼과 개별 이미지 조정 기능이 다시 표시된다. 흐름 목록·카드 순서 조작은 복구하지 않고 가운데 캔버스에 그대로 둔다.
- 범위: 선택 카드에 연결된 이미지만 편집기에 표시해 다른 이미지 카드의 연결 목록이 섞이지 않게 했다.
- 검증: 런타임 기준 TypeScript, Vitest 17건, Vite production build, Electron 포터블 EXE 패키징 통과. EXE `MZ` 헤더와 SHA-256 확인.
- 배포 파일: `내일스패셜-캔버스-1.0.0-이미지레이아웃복구-portable.exe`, 89,541,506 bytes, SHA-256 `1E50B85E7F9F25004A7E2E2B9CF389C645F0C5BEDD9B0EC4D6B3BA90B0C78C6C`

## 2026-07-22 이미지 캔버스 정보 라벨·기본 카드 삭제

- 변경: 이미지 라이브러리의 파일명·URL 라벨을 제거하고, 가운데 캔버스의 실제 이미지마다 `이미지 01`과 파일명 또는 URL을 겹쳐 표시했다. 라벨 텍스트는 선택·복사할 수 있다.
- 변경: 이미지 흐름에서 기본 텍스트·기본 이미지를 포함한 모든 카드를 선택하면 동일한 삭제 버튼이 표시된다. 기본 카드를 모두 지워도 하단의 텍스트/이미지 추가 버튼으로 빈 흐름부터 다시 구성할 수 있다.
- 검증: 런타임 기준 TypeScript, Vitest 17건, Vite production build, Electron 포터블 EXE 패키징 통과. EXE `MZ` 헤더와 SHA-256 확인.
- 배포 파일: `내일스패셜-캔버스-1.0.0-이미지정보-기본카드삭제-portable.exe`, 89,541,696 bytes, SHA-256 `2CAF98606A7DFB174D82094889FEC7D3B78CC87DDCF3B18846597EF9CD7FD19E`

## 2026-07-22 이미지 흐름 선택 경계선 제거

- 변경: 이미지 흐름 블록을 선택하거나 마우스를 올렸을 때 카드 추가 영역까지 가로지르던 청록색 외곽선을 제거했다. 카드 자체를 선택했을 때의 카드 도구와 선택 상태는 유지된다.
- 배포: 사용자 지시에 따라 이번 수정은 EXE를 빌드하지 않았다.

## 2026-07-22 반복 프리셋 카드 편집 통일

- 변경: `목록`, `가격·특전`, `정보표`, `아이콘 카드`, `캡션 이미지 그리드`, `좌우 추천 메뉴`, `세로 타임라인`의 반복 항목을 가운데 캔버스에서 직접 선택하도록 통일했다.
- 조작: 선택한 항목에는 위·아래 이동과 삭제 도구가 표시되고, 블록 하단에서 항목·행·카드를 추가한다. 오른쪽 패널은 전체 항목 목록 대신 선택한 한 항목의 구조화된 입력 필드만 보여 준다.
- 이미지: 그리드·추천 메뉴·타임라인도 가운데 이미지 위에 `이미지 위치 + 파일명 또는 URL` 라벨을 표시해 원본을 바로 확인·복사할 수 있다.
- 범위: 단일 텍스트 블록은 반복 항목이 아니므로 자유 박스 편집 방식을 유지한다.
- 검증: 런타임 기준 TypeScript, Vitest 17건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. 사용자 지시에 따라 EXE는 빌드하지 않았다.

## 2026-07-22 그리드 프리셋 제거

- 변경: 새 블록을 추가하는 프리셋 라이브러리에서 `캡션 이미지 그리드`를 제거했다. 남은 레퍼런스 프리셋은 `좌우 추천 메뉴`, `세로 타임라인`이다.
- 호환: 이미 저장된 그리드 블록은 열기·내보내기가 끊기지 않도록 호환 렌더러는 유지한다. 다만 새 프로젝트에서는 그리드 프리셋을 추가할 수 없다.
- 검증: 런타임 기준 TypeScript, Vitest 18건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 블록 편집 격자선 제거

- 정정: 사용자가 제거를 요청한 대상은 프리셋 이름이 아니라 선택한 블록 안에 보이던 12열 편집 격자선이었다.
- 변경: 블록의 드래그·크기 조절 계산은 유지하고, 캔버스 배경의 세로·가로 격자선은 항상 표시하지 않도록 했다.
- 검증: 런타임 기준 TypeScript, Vitest 18건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 텍스트 프리셋 흐름형 편집 전환

- 변경: `텍스트` 프리셋에서 자유 박스 배치와 이미지 박스 추가 기능을 제거했다. 텍스트는 기본 카드와 추가 텍스트 카드가 위에서 아래로 이어지는 흐름으로 표시된다.
- 조작: 블록 하단에 `텍스트 추가` 버튼을 신설했다. 추가 카드는 선택 후 위·아래 이동과 삭제가 가능하며, 선택한 카드의 상단 분류·제목·본문만 오른쪽 패널에서 편집한다.
- 패널: 텍스트 프리셋 선택 시 오른쪽의 `박스 배치` 메뉴·좌표·크기 조절 UI를 표시하지 않는다.
- 검증: 런타임 기준 TypeScript, Vitest 18건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 전체 프리셋 자유 박스 배치 제거

- 변경: 모든 프리셋에서 블록 내부 박스의 드래그 이동·크기 조절·좌표·레이어 순서와 텍스트/이미지 박스 추가 UI를 제거했다.
- 레이아웃: 목록·정보표·아이콘 카드·가격·특전 등 일반 프리셋도 자유 배치 캔버스가 아닌 프리셋 고유의 고정 콘텐츠 구조로 표시한다. 반복 항목은 가운데 선택·추가·순서 변경·삭제 방식만 사용한다.
- 유지: 이미지 안의 사진 배열·강조·크롭을 조절하는 `이미지 내부 레이아웃`은 별도 기능이므로 유지했다.
- 호환: 저장된 기존 `layoutBoxes` 데이터는 열기·내보내기 호환을 위해 보존하지만, 편집 화면에는 자유 배치 도구를 다시 노출하지 않는다.
- 검증: 런타임 기준 TypeScript, Vitest 18건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 페이지 설정 제거·공통 텍스트 더미 통일

- 제거: 블록을 선택하지 않았을 때 표시되던 `페이지 설정`의 대표 제목·부제·여행지·카테고리·레이아웃·전달 단계·내부 메모 입력을 없앴다. 선택 전 오른쪽 패널은 블록 선택 안내만 표시한다.
- 통일: 모든 새 프리셋의 기본 텍스트를 `Category Label` / `제목` / `본문`으로 통일했다. 기존 저장본의 정확한 기본 더미 문구도 불러올 때 같은 공통 문구로 정리한다.
- 텍스트: `텍스트 추가`로 만든 카드도 기본 텍스트와 같은 상단 분류·제목·본문 시각 양식으로 표시하고, 순서 이동이 실제 텍스트 흐름에 적용되도록 보정했다.
- 검증: 런타임 기준 TypeScript, Vitest 18건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 목록 특전형 강조 스위치

- 변경: `목록` 블록의 항목 구조는 유지한 채, 오른쪽 패널에서 `특전형 강조` 스위치를 켜면 청록색 라운드 바 형태의 가격·특전 표현으로 바뀌게 했다.
- 보존: 스위치를 다시 끄면 일반 번호 목록으로 되돌아가며, 제목·본문·항목 데이터는 변경하지 않는다. 선택한 표현 값은 프로젝트·매니페스트·캔버스 전달 JSON에 함께 저장한다.
- 검증: 런타임 기준 TypeScript, Vitest 18건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 일정표 명칭·자동 DAY 추가·블록 검색 제거

- 변경: 블록 라이브러리와 캔버스의 `정보표` 명칭을 `일정표`로 변경했다.
- 자동화: 일정표에서 `행 추가`를 누르면 마지막 구분 값의 `DAY` 숫자를 읽어 다음 행에 `DAY N+1`을 자동 입력한다. 숫자를 읽지 못하는 경우에는 현재 행 수 기준으로 다음 DAY를 만든다.
- 정리: 왼쪽 블록 라이브러리의 검색 입력을 제거하고 모든 블록을 바로 표시한다.
- 검증: 런타임 기준 TypeScript, Vitest 18건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 블록 구조 드롭다운 제거·다음 블록 선택 팝업

- 제거: 오른쪽 편집 패널의 `블록 구조` 드롭다운을 제거해 기존 블록의 타입을 편집 중에 바꾸지 않도록 했다.
- 추가: 캔버스 하단 `다음 블록 추가` 버튼을 누르면 공통 블록·레퍼런스 레이아웃 목록이 나오는 선택 팝업을 표시한다. 선택한 블록은 시안 마지막에 추가되고 자동 선택된다.
- 검증: 런타임 기준 TypeScript, Vitest 18건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 목록 항목 단건 편집·공통 텍스트 유지

- 변경: 목록 블록을 선택하면 첫 항목을 자동 선택해 오른쪽 패널에 선택 항목 편집 필드를 바로 표시한다. 가운데에서 다른 항목을 클릭하면 해당 항목 편집으로 전환된다.
- 유지: 목록의 `Category Label`·`제목`·`본문` 공통 텍스트 필드는 선택 항목 편집 화면 상단에 계속 표시한다.
- 정리: 줄바꿈으로 전체 항목을 한 번에 입력하는 `한 줄에 항목 하나씩` 필드는 화면에서 제거하고, `특전형 강조` 스위치는 항목 편집 중에도 유지했다.
- 검증: 런타임 기준 TypeScript, Vitest 18건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 블록 라이브러리 안내 문구 제거

- 제거: 왼쪽 블록 라이브러리의 `CONTENT BLOCKS`, `블록`, 블록 개수와 블록 사용 안내 문구를 제거했다.
- 유지: 공통 블록·레퍼런스 레이아웃 구분 및 각 블록 버튼명은 선택 편의를 위해 유지했다.
- 검증: 런타임 기준 TypeScript, Vitest 18건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 오른쪽 검수 패널 제거

- 제거: 오른쪽 패널의 `디자인/검수` 탭 구조와 `검수` 패널을 제거하고, 오른쪽은 선택 블록 편집만 표시하도록 정리했다.
- 내보내기: 고객 최종본 상태에서 검수 오류 때문에 HTML/ZIP 내보내기를 막던 화면 흐름을 제거했다.
- 검증: 런타임 기준 TypeScript, Vitest 18건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 캔버스 더블클릭 텍스트 직접 편집

- 변경: 가운데 캔버스에 보이는 상단 분류·제목·본문·목록 항목·일정표 헤더/셀·아이콘 카드·레퍼런스 레이아웃 문구를 더블클릭하면 즉시 입력창으로 전환되도록 했다.
- 편집: Enter 또는 바깥 클릭으로 저장, Esc로 취소한다. 본문형 여러 줄 텍스트는 Ctrl+Enter로 저장하고 일반 Enter는 줄바꿈으로 사용한다.
- 범위: 오른쪽 패널 편집 필드는 유지하되, 실제 시안 위에서 바로 수정할 수 있게 해 반복적인 패널 왕복을 줄였다.
- 검증: 런타임 기준 TypeScript, Vitest 18건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 목록 프리셋 병목 개선

- 제거: 새 블록 라이브러리와 `다음 블록 선택` 팝업에서 `가격·특전` 프리셋을 제거했다. 기존 저장본의 `offer` 타입은 열기·렌더·내보내기 호환을 유지한다.
- 추가: 선택 항목 도구에 `위에 추가`, `아래에 추가`, `복제`를 더해 중간 삽입과 반복 문구 작성이 바로 가능해졌다.
- 개선: 목록 블록 선택 시 첫 항목을 자동 선택하고, 항목 삭제 후에도 남은 인접 항목을 계속 선택해 편집 흐름이 끊기지 않도록 했다.
- 검증: 런타임 기준 TypeScript, Vitest 18건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 일정표 일괄 행 입력 제거

- 제거: 일정표 프리셋 오른쪽 패널의 `한 줄에 한 행, | 로 열 구분` 일괄 입력 박스를 제거했다.
- 유지: 표 열 수 선택, 열 제목 편집, 가운데 캔버스에서 행 선택 후 단건 편집하는 흐름은 유지했다.
- 검증: 런타임 기준 TypeScript, Vitest 18건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 CI 및 앱 명칭 변경

- 변경: 상단 브랜드 마크를 `C:\Users\naeiltour0001\Downloads\1.내일투어.png` 기반 CI 이미지로 교체하고, 앱 표시명을 `내일스패셜 메이킹 스튜디오`로 변경했다.
- 반영: 브라우저 탭 제목, 빈 캔버스 안내 문구, Electron 창 제목과 productName도 같은 명칭으로 맞췄다.
- 자산: CI 이미지는 `src/assets/naeil-tour-ci.png`로 복사해 Vite 빌드 자산으로 포함했다.
- 검증: 런타임 기준 TypeScript, Vitest 18건, Vite production build, `http://127.0.0.1:4173` 응답 200과 UTF-8 기준 탭 제목 반영을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 좌우 추천 메뉴·세로 타임라인 편집 통합

- 통일: 두 레퍼런스 프리셋도 블록 선택 시 첫 항목을 자동 선택하고, 오른쪽 패널에서 공통 `Category Label`·`제목`·`본문`과 선택 항목 필드를 함께 편집하도록 맞췄다. 기존의 줄바꿈 일괄 입력 UI는 제거했다.
- 이미지: 가운데의 대표·항목 이미지를 클릭하면 해당 이미지 자리만 선택된다. 왼쪽 라이브러리에서 파일 또는 URL 이미지를 연결·교체할 수 있으며, 이미지 파일명·URL은 가운데와 오른쪽에서 확인·복사할 수 있다.
- 정합성: 추천 메뉴·타임라인의 항목을 위/아래 삽입, 복제, 이동, 삭제할 때 대응 이미지도 함께 삽입·복제·이동·삭제되게 했다. 타임라인은 대표 이미지 슬롯을 별도로 보존한다.
- 정리: 세로 타임라인 제목의 고정 `상품 특전` 접미사를 제거해 입력한 `제목`을 그대로 사용한다. 레퍼런스의 시각 레이아웃 자체는 유지한다.
- 검증: 런타임 기준 TypeScript, Vitest 19건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 레퍼런스 프리셋 공통 텍스트 헤더 통일

- 구조: `좌우 추천 메뉴`와 `세로 타임라인`의 상단을 다른 블록과 같은 `Category Label → 제목 → 본문` 순서·서체·여백으로 통일했다.
- 분리: 좌우 이미지·텍스트 반복과 대표 이미지·세로 시간축은 공통 텍스트 영역 아래의 전용 레이아웃으로만 남겼다. 타임라인의 카테고리 캡슐·본문 분할과 추천 메뉴의 결합형 헤더는 제거했다.
- 내보내기: 독립 HTML도 캔버스와 같은 공통 텍스트 헤더를 먼저 렌더하도록 맞췄다.
- 검증: 런타임 기준 TypeScript, Vitest 20건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 캔버스 이미지 선택 자동 라이브러리 전환

- 선택: 일반 블록·이미지 흐름·레퍼런스 레이아웃에서 가운데 이미지 박스를 클릭하면 왼쪽 패널이 즉시 `이미지` 탭으로 전환된다. 선택한 이미지 박스 또는 레퍼런스 슬롯이 파일·URL 연결 대상으로 유지된다.
- 패널: 일반 이미지 박스는 오른쪽에 해당 박스의 `이미지 내부 레이아웃`을 바로 표시한다. 레퍼런스 프리셋은 고정된 디자인 위치를 명시하고, 선택 이미지의 `크롭 위치`를 조절할 수 있게 했다.
- 표시: 선택한 일반 이미지 박스에는 청록색 선택선을 표시하고, 레퍼런스 이미지도 설정한 크롭 위치가 가운데 시안에 즉시 반영된다.
- 검증: 런타임 기준 TypeScript, Vitest 20건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 레퍼런스 프리셋 공통 헤더 스타일 충돌 수정

- 원인: `좌우 추천 메뉴`의 노란 밑줄과 `세로 타임라인`의 주황 제목 효과가 공통 헤더의 제목 내부 줄바꿈 `span`에도 상속되고, 추천 메뉴 본문 규칙이 공통 본문 글자 크기까지 덮어쓰고 있었다.
- 수정: 두 레퍼런스 프리셋의 `Category Label → 제목 → 본문` 영역은 모든 일반 프리셋과 같은 제목 색·서체·크기·행간 및 본문 크기로 강제 통일했다. 전용 색·밑줄 효과는 반복 항목의 제목에만 남는다.
- 내보내기: 독립 HTML도 공통 본문 크기와 행간을 같은 값으로 맞췄다.
- 검증: 런타임 기준 TypeScript, Vitest 20건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 세로 타임라인 이미지 패널 단순화·추천 메뉴 기본 항목 축소

- 세로 타임라인: 대표·일정 이미지를 클릭해도 오른쪽에 별도 이미지 편집·크롭·레이아웃 카드를 열지 않는다. 대표 이미지는 공통 텍스트 편집 패널을, 일정 이미지는 해당 일정 항목의 텍스트 편집 패널을 유지한다. 왼쪽 이미지 탭의 연결 대상은 그대로 유지한다.
- 좌우 추천 메뉴: 새 블록을 만들 때 반복 추천 항목은 기본 1개만 생성한다. 저장돼 있던 정확히 같은 기본 더미 2개도 열 때 1개로 정리하며, 사용자가 직접 작성한 2개 항목은 보존한다. 필요한 경우 캔버스 하단 `항목 추가`로 늘린다.
- 검증: 런타임 기준 TypeScript, Vitest 20건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 좌우 추천 메뉴 추가 항목 기본값 통일

- 수정: 좌우 추천 메뉴에서 `항목 추가`·위/아래 삽입으로 만드는 콘텐츠도 처음 블록에 들어가는 `메뉴 제목 | 메뉴 설명을 입력하세요.` 기본값과 동일하게 통일했다.
- 검증: 런타임 기준 TypeScript, Vitest 20건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 캔버스 이미지 출처 라벨 단순화

- 수정: 가운데 이미지에 붙는 `추천 이미지`, `일정 이미지`, `이미지 01` 같은 접두어를 제거했다. 이제 URL로 추가한 이미지는 전체 URL만, 파일 업로드 이미지는 파일명만 표시한다.
- 표시: 모든 이미지 라벨의 말줄임표를 제거하고 긴 URL도 줄바꿈으로 끝까지 보이게 했다. 텍스트 선택·복사도 그대로 가능하다.
- 검증: 런타임 기준 TypeScript, Vitest 20건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 일정 블록 명칭 변경

- 변경: 블록 명칭 `일정표`를 `간략 일정표`로, `세로 타임라인`을 `상세 일정표`로 변경했다. 블록 라이브러리, 선택 팝업, 캔버스 태그, 오른쪽 패널 제목에 함께 반영된다.
- 검증: 런타임 기준 TypeScript, Vitest 20건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 좌우 추천 메뉴 반복 항목 양식 통일

- 레이아웃: 추천 메뉴의 두 번째 이후 항목도 첫 기본 항목과 같은 왼쪽 이미지·오른쪽 텍스트 위치 및 이미지 크기로 고정했다. 이전의 좌우 반전 규칙은 제거했다.
- 텍스트: 반복 항목 편집 필드 `강조 제목`을 `제목`으로 바꾸고, 캔버스와 독립 HTML의 노란 형광 배경·빨간 글자 강조를 일반 제목 스타일로 제거했다.
- 검증: 런타임 기준 TypeScript, Vitest 20건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 상세 일정표 이미지 선택 패널 고정

- 수정: 상세 일정표의 대표 이미지와 일정 이미지를 클릭해도 오른쪽 `선택 이미지 편집` 패널이 나타나지 않는다. 항상 `선택 항목 편집`을 표시하며, 대표 이미지는 첫 일정 항목을 선택 상태로 연결한다.
- 유지: 왼쪽 이미지 라이브러리에서는 클릭한 이미지 자리를 계속 교체할 수 있다.
- 검증: 런타임 기준 TypeScript, Vitest 20건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 좌우 추천 메뉴 항목 더미 텍스트 공통화

- 수정: 좌우 추천 메뉴의 최초 항목·추가 항목·삽입 항목을 모두 `제목 | 본문`으로 생성한다. 오른쪽 선택 항목 편집 필드도 `제목`·`본문`으로 통일했다.
- 호환: 기존의 정확한 기본 더미 문구만 `제목 | 본문`으로 자동 정리하며, 사용자가 작성한 추천 메뉴 항목은 보존한다.
- 검증: 런타임 기준 TypeScript, Vitest 20건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 이미지·설명 카드 반전 및 공통 텍스트 보완

- 명칭: 레퍼런스 레이아웃 `좌우 추천 메뉴`를 `이미지·설명 카드`로 변경했다.
- 카드 도구: 항목 도구의 위·아래 삽입 `+` 버튼을 제거하고, 이미지와 텍스트 위치를 바꾸는 반전 버튼으로 대체했다. 반전 상태는 항목 복제·이동·삭제와 독립 HTML 내보내기에도 유지된다.
- 공통 텍스트: 간략 일정표와 아이콘 카드도 행·카드 선택 상태에서 공통 텍스트 영역을 함께 표시해 `Category Label`·제목·본문 편집이 끊기지 않게 했다.
- 검증: 런타임 기준 TypeScript, Vitest 20건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 공통 텍스트 즉시 편집 상태 표시

- 편집: `공통 텍스트`의 카테고리 라벨·제목·본문을 클릭하면 기존 더미 문구가 전체 선택되어 바로 입력으로 바꿀 수 있다.
- 표시: 편집 중인 공통 텍스트 카드는 파란 테두리·연한 파란 배경·필드 라벨 강조로 활성 상태를 명확히 보여 준다.
- 검증: 런타임 기준 TypeScript, Vitest 20건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 상세 일정표 일차 확장 구조

- 배경: 상세 일정표의 베이지색 일정 영역 배경을 흰색으로 제거했다.
- 버튼: 기존 `항목 추가`를 `일정 추가`와 `일차 추가`로 분리했다. 일정 추가는 현재 마지막 일차에 빈 일정 한 줄을 더한다.
- 일차: 처음 일차 추가 시 기존 기본 일정을 `1일차`로 편입하고, 빈 일정이 포함된 `2일차`를 생성한다. 이후에는 다음 일차를 이어 생성하며, 일차 구분은 저장·내보내기에도 유지된다.
- 검증: 런타임 기준 TypeScript, Vitest 20건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 레퍼런스 이미지 편집 패널 완전 제거

- 수정: 좌우 추천 메뉴·상세 일정표·캡션 이미지 그리드의 이미지 클릭 시 오른쪽 `선택 이미지 편집` 분기를 완전히 제거했다. 클릭한 이미지에 대응하는 반복 항목의 `선택 항목 편집`만 표시한다.
- 유지: 왼쪽 이미지 라이브러리는 클릭한 이미지 슬롯을 계속 대상으로 삼아 파일·URL 교체가 가능하다.
- 검증: 런타임 기준 TypeScript, Vitest 20건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 상세 일정표 이미지·일차 삭제

- 이미지: 대표 이미지와 각 일정 이미지를 선택하면 `이미지 삭제` 버튼이 나타난다. 버튼은 왼쪽 라이브러리 원본은 유지하고, 해당 이미지 자리의 연결만 해제한다.
- 일차: 다일차 일정의 각 일차 헤더에 `일차 삭제` 버튼을 추가했다. 선택한 일차의 일정·연결 이미지 자리만 함께 제거하고, 마지막 한 일차는 삭제되지 않는다.
- 검증: 런타임 기준 TypeScript, Vitest 20건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 이미지 분할 기능 제거

- 제거: 왼쪽 이미지 패널의 `이미지 분할` 선택창, 가로·세로·2×2 분할 버튼과 관련 안내 문구를 제거했다.
- 보존: 기존 라이브러리에 이미 추가된 분할 이미지는 삭제하지 않는다.
- 검증: 런타임 기준 TypeScript, Vitest 20건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 공통 텍스트 빈 필드 자동 접기

- 캔버스: 공통 텍스트의 카테고리 라벨·제목·본문 중 빈 필드는 출력하지 않는다. 남은 텍스트는 바로 위로 붙고, 첫·마지막 텍스트의 불필요한 위·아래 여백도 제거한다.
- 내보내기: 독립 HTML에서도 같은 규칙으로 빈 필드를 제외한다.
- 검증: 런타임 기준 TypeScript, Vitest 20건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 공통 텍스트 최소 리치 텍스트

- 범위: 공통 텍스트의 `제목`과 `본문`에만 리치 텍스트 편집기를 적용하고, `Category Label`은 일반 텍스트로 유지했다.
- 서식: 선택 문장에 굵게, 글자색(기본·청록·코랄·주황), 글자 크기(소·기본·크게)를 적용할 수 있다.
- 보존: 허용한 서식만 정리해 저장하며 캔버스의 더블 클릭 편집, 블록 복제·저장·불러오기 및 독립 HTML 내보내기에도 유지된다.
- 검증: 런타임 기준 TypeScript, Vitest 20건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 텍스트·이미지 프리셋 리치 텍스트 연결

- 보완: 텍스트·이미지 프리셋의 기본 텍스트 박스와 추가 텍스트 박스도 제목·본문 리치 텍스트 편집기를 사용하도록 통일했다.
- 표시: 선택 카드의 캔버스 렌더링도 같은 서식을 반영하며, 빈 필드는 기존 자동 접기 규칙을 적용한다.
- 검증: 런타임 기준 TypeScript, Vitest 20건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 리치 텍스트 서식 적용성 보완

- 수정: 브라우저가 색상을 RGB 형식으로 저장해 제거되던 문제를 정리 단계에서 허용 색상값으로 변환해 보존한다.
- 사용성: 문장을 드래그하면 선택 영역에만 적용되고, 선택하지 않고 누르면 제목·본문 전체에 적용된다. 크기 차이도 소 78%·기본 100%·크게 132%로 명확히 조정했다.
- 검증: 런타임 기준 TypeScript, Vitest 20건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 상세 일정표 히어로 이미지 삭제 상시 노출

- 수정: 상세 일정표의 연결된 히어로 이미지 우측 상단에 선택 여부와 무관하게 `히어로 이미지 삭제` 버튼을 표시한다.
- 동작: 버튼은 왼쪽 이미지 라이브러리 원본을 삭제하지 않고 히어로 이미지 자리 연결만 해제한다.
- 검증: 런타임 기준 TypeScript, Vitest 20건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 상세 일정표 히어로 영역 삭제로 정정

- 정정: 앞선 구현은 이미지 연결 해제로 해석이 달랐다. 요구한 동작은 큰 히어로 이미지 박스 자체를 삭제하는 것이다.
- 수정: 빈 자리·연결 이미지 모두에 `히어로 이미지 삭제`를 표시하고, 누르면 큰 히어로 영역을 캔버스·독립 HTML에서 제거해 일정 목록이 위로 붙도록 했다. 연결 정보는 보존한다.
- 호환: 저장·불러오기 및 매니페스트의 `timeline_hero_visible` 상태를 함께 유지한다.
- 검증: 런타임 기준 TypeScript, Vitest 20건, Vite production build, `http://127.0.0.1:4173` 응답 200과 최종 TypeScript 재검증을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 미리보기 팝업 및 디자이너 전달 ZIP 정리

- 미리보기: 상단 `미리보기`를 별도 내보내기 대신 앱 안의 전체 시안 팝업으로 변경했다. 팝업 안에서 720px 완성 시안을 스크롤해 전체 내용만 확인할 수 있다.
- 내보내기: 메뉴를 `독립 HTML`, `로드용 JSON 파일`, `디자이너 전달 ZIP` 세 가지로만 정리했다. 로드용 JSON은 이미지 연결까지 유지해 스튜디오에서 다시 불러올 수 있다.
- 디자이너 전달 ZIP: `index.html`, `assets/` 이미지, `로드용-프로젝트.json`, 전달 메모를 함께 패키징한다. URL로 넣은 이미지는 다운로드해 로컬 `assets/` 경로로 HTML을 교체한다. 원본 서버 접근 제한 등으로 다운로드하지 못한 URL만 `이미지-다운로드-확인필요.md`에 기록하고, 온라인 확인용 원본 URL은 HTML에 유지한다.
- 검증: 런타임 기준 TypeScript, Vitest 20건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 공통 텍스트 한글 입력 커서 밀림 수정

- 원인: 공통 텍스트 리치 에디터가 매 입력마다 `innerHTML`을 다시 주입해 한글 IME 조합과 커서 위치가 흔들렸다.
- 수정: 편집 중인 `contentEditable` DOM은 유지하고, 외부 값 동기화는 포커스 밖일 때만 수행하도록 분리했다. 입력 중에도 캔버스 값은 갱신되지만 커서·조합 문자열은 브라우저가 유지한다.
- 검증: 런타임 기준 TypeScript, Vitest 20건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 블록 라이브러리 배열 단순화

- 제거: 왼쪽 블록 라이브러리와 다음 블록 선택 팝업의 `레퍼런스 레이아웃` 분류 텍스트를 제거했다.
- 배열: `텍스트 / 이미지`, `목록 / 아이콘 카드`, `간략 일정표 / 상세 일정표`, `이미지·설명 카드` 순서의 2열 블록 배열로 통일했다.
- 범위: 기존 캡션 이미지 그리드·가격 특전 블록 데이터는 저장본 호환을 위해 유지하되, 새 블록 선택 목록에서는 제외한다.
- 검증: 런타임 기준 TypeScript, Vitest 20건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 이미지·설명 카드 아이콘 교체

- 수정: 이미지·설명 카드 블록의 반짝이 아이콘을 이미지와 설명 패널이 나란히 보이는 레이아웃 아이콘으로 교체했다.
- 검증: 런타임 기준 TypeScript, Vitest 20건, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 HTML 이미지 출처 라벨

- 적용: 독립 HTML과 디자이너 전달 ZIP의 `index.html`에서 모든 이미지 하단에 출처 라벨을 표시한다.
- 표시값: URL로 등록한 이미지는 전체 URL, 파일 업로드 이미지는 원본 파일명으로 표시한다. ZIP 패키징 시 URL 이미지를 로컬 파일로 바꾸더라도 라벨에는 원본 URL을 보존한다.
- 검증: 런타임 기준 TypeScript, Vitest 20건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 빌드하지 않았다.

## 2026-07-22 EXE URL 이미지 네이티브 다운로드 경로

- 원인: 브라우저에서 ZIP을 만들 때 외부 URL 이미지 다운로드가 CORS 정책에 막릴 수 있다.
- 수정: Python `requests`를 별도 패키징하는 대신 Electron 메인 프로세스의 네이티브 `net.fetch`를 IPC로 연결했다. EXE에서는 브라우저 CORS를 거치지 않고 URL 이미지를 받아 `assets/`에 넣는다. 웹 실행 환경에서는 기존 `fetch`를 보조 경로로 유지한다.
- 패키징: `preload.cjs`를 추가하고 EXE 빌드 스크립트·electron-builder 파일 목록에 포함했다.
- 검증: Electron main/preload 문법 검사, 런타임 TypeScript, Vitest 20건, Vite production build, `http://127.0.0.1:4173` 응답 200을 확인했다. EXE는 사용자 확인 전 빌드하지 않았다.

## 2026-07-22 포터블 EXE 빌드

- 생성: `release/내일스패셜-캔버스-1.0.0-portable.exe`를 새 코드로 빌드했다.
- 포함: URL 이미지 네이티브 다운로드용 `preload.cjs`와 Electron IPC 경로가 `app.asar`에 포함된 것을 확인했다.
- 검증: 제공 빌드 배치 파일 실행, TypeScript, Vitest 20건, Vite production build, electron-builder Windows x64 portable 패키징, SHA-256 일치를 확인했다.
- SHA-256: `3D7F4BE80B43F6DEC789A4E850EA7BECC8FF34C1A1D004CFFD1DAF08B10487E3`

## 2026-07-22 URL 이미지 확장자 감지 보완

- 확인: 사용자가 전달한 `4153500f-...img` 파일은 다운로드 실패가 아니라 JPEG 헤더(`FF D8 FF E0`)를 가진 596×335 이미지였다. 원본 URL에 확장자가 없어 `.img`로 저장된 문제였다.
- 수정: URL 확장자가 없을 때 Electron·브라우저 다운로드 응답의 MIME 타입(`image/jpeg`, `image/png`, `image/webp` 등)을 읽어 실제 확장자로 저장하도록 변경했다.
- 검증: 런타임 TypeScript, Vitest 20건, Vite production build를 확인했다. 로컬 미리보기 서버는 실행 중이 아니어서 HTTP 확인은 생략했다. EXE 재빌드는 사용자 확인 대기 상태다.

## 2026-07-22 URL 이미지 확장자 범위 제한

- 조정: 다운로드 응답 확장자 판별은 `.jpg`와 `.png`만 사용하도록 제한했다. 그 외 형식은 억지로 확장자를 바꾸지 않고 `.img`로 유지한다.
- 검증: 런타임 TypeScript와 Vitest 20건을 확인했다. EXE 재빌드는 사용자 확인 대기 상태다.

## 2026-07-22 디자이너 ZIP 이미지 JPG·PNG 실제 변환 및 재빌드

- 정정: 단순 확장자 판별이 아니라 ZIP에 넣기 전 모든 이미지를 Canvas로 실제 재인코딩하도록 변경했다.
- 결과: 원본 JPEG는 `.jpg`, PNG·WEBP·GIF 등 나머지 형식은 `.png`로 변환한다. ZIP `assets/`에는 `.jpg`와 `.png`만 남는다.
- 생성: `release/내일스패셜-캔버스-1.0.0-portable.exe`를 재빌드했다.
- 검증: 제공 빌드 배치 파일, TypeScript, Vitest 20건, Vite production build, electron-builder Windows x64 portable 패키징 및 app.asar 최신 번들·preload 포함을 확인했다.
- SHA-256: `2BEEBE329508922099C5641C7B378683F2F8F5D30CD9C2DF3A7AC29321B4F8D5`

## 2026-07-22 이전 EXE 정리

- 정리: `release` 폴더에서 이전 포터블 EXE 11개를 삭제하고 최신 `내일스패셜-캔버스-1.0.0-portable.exe`만 남겼다.
- 보존: `SHA256SUMS.txt`는 유지했다.

## 2026-07-22 AI 자동작성 계약·Skill 기반 구현

- 계약 원본: `schema/`에 Project·approved brief JSON Schema, 스키마/카탈로그 버전, 블록 카탈로그, 계약 manifest를 추가했다. 신규 생성 7개 블록과 호환 전용 `offer`·`caption-grid`를 분리했다.
- 자동 동기화: `scripts/contracts.mjs`와 `contract:sync`·`contract:check`를 만들었다. 앱 빌드 배치가 계약 SHA-256을 검증한 뒤 `.agents/skills/naeil-special-canvas-writer/references/canvas-contract/`로 전체 계약을 동기화한다.
- 호환성: `2.0`·무버전 프로젝트를 `2.0.0`으로 변환하고 IndexedDB 백업·migrationLog를 남긴다. 미래 블록은 안내 블록으로 표시하되 로드 JSON으로 다시 저장할 때 원본 type·필드를 복원한다.
- Skill: 승인 상태의 brief만 현재 카탈로그에 맞는 Project JSON으로 생성하는 `naeil-special-canvas-writer`를 초기화했다. draft brief 차단, 생성 JSON 검증, 이미지 다운로드 상태·확인 필요 목록 전달을 포함한다.
- 검증: 계약 sync/check 통과, Skill 구조 검증 통과, 승인 brief 생성·검증 성공, draft brief 생성 차단, TypeScript·Vitest 26건·Vite production build 통과. EXE는 사용자 확인 전 빌드하지 않았다.

## 2026-07-22 실상품 레퍼런스 생성 검증

- 대상: 임시작업폴더의 실제 내일스패셜 레퍼런스 이미지로 금까기 3건·우리만 2건·특별한 2건·골프 2건을 생성·검증했다.
- 결과: 9건 모두 현재 계약 JSON 생성과 Skill 검증을 통과했다. 각 이미지 파일은 data URI로 연결해 로컬 로드 가능성을 확인했다.
- 사실 보호: 레퍼런스 이미지에 가격·일정·포함 조건 원문이 없으므로 모든 생성본의 해당 정보는 `needsReview`로 남겼으며, 임의 판매 문구를 만들지 않았다.
- 추가 검증: 승인 brief 생성 JSON이 앱 정규화·독립 HTML 경로에서 열리고, 이미지 다운로드 상태·생성 메타데이터가 재로드 JSON에 보존되는 회귀 테스트를 추가했다. TypeScript·Vitest 28건·Vite production build 통과. EXE는 빌드하지 않았다.

## 2026-07-22 디자이너 전달 ZIP 실패 기록 회귀 검증

- 검증 추가: 접근 제한 URL 이미지를 모의 입력해 `이미지-패키징-결과.json`, 원본 URL `.url.txt`, `이미지-다운로드-확인필요.md`, 독립 HTML이 모두 ZIP 내부에 생성되는지 확인했다.
- 결과: 다운로드 실패 사유와 원본 URL이 ZIP에 보존되며, 실패 상태가 패키징 결과 파일에 기록된다.

## 2026-07-23 공통 텍스트 통합 EXE 빌드

- 반영: 모든 프리셋의 공통 텍스트를 같은 `Category Label / 제목 / 본문` 렌더링·편집 구조로 통합하고, 서식만 남은 빈 리치 텍스트도 자동 접히도록 보완했다.
- 생성: `release/내일스패셜-캔버스-1.0.0-portable.exe` 포터블 EXE를 최신 소스로 빌드했다.
- 검증: 계약 동기화·검사, TypeScript, Vitest 29건, Vite production build, Electron Windows x64 portable 패키징을 통과했다.
- SHA-256: `2800CD926B723207F33BF9B516CEFE4999347EB40DF072EC7421824279070BAC`
- 최종 검증: 계약 sync/check, TypeScript, Vitest 29건, Vite production build, writer Skill 구조 검증, 실제 레퍼런스 9건 생성·검증 통과. EXE는 빌드하지 않았다.

## 2026-07-23 AI 작성 3단계 앱 연동 완료

- 1단계: 상단 `AI 작성`에서 상품 기본 정보, 원본 자료, URL, 사실 확인 상태, 사용할 이미지를 `briefWorkspace`에 저장하도록 구현했다.
- 2단계: 입력 자료를 draft brief로 구조화하고, 블록·공통 텍스트·확인 필요 항목을 검토·수정할 수 있게 구현했다. 원문이나 이미지가 없는 항목은 임의 생성하지 않고 `needsReview`로 남긴다.
- 3단계: 승인된 brief만 `internal-draft` Project JSON으로 변환해 현재 캔버스에 반영한다. 일반 로드 JSON과 디자이너 ZIP에서는 원본 `briefWorkspace`를 제외한다.
- 계약: Project schema와 migration을 `2.1.0`으로 올리고 앱·writer Skill 계약을 동기화했다.
- 검증: contract sync/check, TypeScript, Vitest 31건, Vite production build, writer Skill 구조 검증, 브라우저에서 자료 입력 → draft → 승인 → 캔버스 4블록 반영을 확인했다. EXE는 사용자 확인 전 빌드하지 않았다.

## 2026-07-23 시작 빈 페이지·종료 저장 확인

- 시작: 자동 저장된 마지막 작업을 곧바로 여는 대신, 앱을 다시 열면 항상 새 빈 기획안으로 시작하도록 변경했다. 기존 작업본은 프로젝트 목록에서 그대로 다시 열 수 있다.
- 종료: EXE 창을 닫을 때 `저장 후 종료 / 저장하지 않고 종료 / 취소` 선택 창을 표시한다. 저장 후 종료는 최신 캔버스 상태를 즉시 로컬 작업공간에 기록한 뒤 닫는다.
- 검증: TypeScript, Vitest 31건, Electron main·preload 문법 검사, 계약 동기화·검사, Vite production build를 통과했다. EXE는 사용자 요청 전 재빌드하지 않았다.

## 2026-07-23 시작·종료 저장 EXE 빌드

- 생성: 시작 시 새 빈 기획안과 종료 저장 확인 동작을 포함한 포터블 EXE를 재빌드했다.
- 검증: 계약 동기화·검사, TypeScript, Vitest 31건, Vite production build, Electron Windows x64 portable 패키징을 통과했다.
- SHA-256: `BCF37F9A28BD9DBC6FEFF084C193EDF49474A7658E2DBF5B0064E9DA753A9AD7`

## 2026-07-23 레이어 클릭 캔버스 이동 및 EXE 빌드

- 수정: 왼쪽 레이어 목록의 단락을 클릭하면 해당 블록을 선택하고, 가운데 캔버스 스크롤 영역에서 해당 위치를 부드럽게 가운데로 이동하도록 연결했다.
- 생성: 변경 사항을 포함한 포터블 EXE를 재빌드했다.
- 검증: 계약 동기화·검사, TypeScript, Vitest 31건, Vite production build, Electron Windows x64 portable 패키징을 통과했다.
- SHA-256: `C71BF26F20593FEF19DFEB051968FCC5D83E7C8BEEE1C3A142801AE01A13BB2B`

## 2026-07-23 텍스트 Enter 줄바꿈 보존 및 EXE 빌드

- 수정: 공통 리치 텍스트에서 Enter 입력 시 브라우저가 만드는 문단 태그를 `<br>`로 보존해 저장·캔버스·독립 HTML에서 줄바꿈이 유지되도록 했다. 여러 줄 일반 텍스트도 캔버스에서 `pre-wrap`으로 표시한다.
- 회귀 검증: Enter 줄바꿈이 리치 텍스트 정리와 독립 HTML 내보내기에서 남는 테스트를 추가했다.
- 생성: 변경 사항을 포함한 포터블 EXE를 재빌드했다.
- 검증: 계약 동기화·검사, TypeScript, Vitest 32건, Vite production build, Electron Windows x64 portable 패키징을 통과했다.
- SHA-256: `6A2692D41A574A976BA2227F1FC679B8724E1BF21BEF4AA72F2D45974561560C`

## 2026-07-23 레이어 콘텐츠 그룹 및 EXE 빌드

- 기능: 왼쪽 레이어에서 연속 블록을 2개 이상 선택해 콘텐츠 그룹으로 묶을 수 있다. 그룹명 편집, 접기·펼치기, 그룹 전체 위·아래 이동, 그룹 해제를 지원한다.
- 안전: 그룹의 개별 블록을 이동·삭제하면 해당 블록을 그룹에서 자동 분리해 그룹의 연속성·캔버스 순서가 어긋나지 않게 했다. 그룹 정보는 로드용 JSON에 보존되며, 독립 HTML 출력에는 영향을 주지 않는다.
- 계약: Project JSON 스키마에 선택적 `contentGroups` 구조를 추가하고 기존 저장본은 빈 그룹으로 호환 처리했다.
- 검증: 콘텐츠 그룹 정규화·로드 JSON 보존 회귀 테스트를 추가했다. 계약 동기화·검사, TypeScript, Vitest 33건, Vite production build, Electron Windows x64 portable 패키징을 통과했다.
- SHA-256: `4A28FBEB2587B7372E4AAA133C173E3B3E959F1E801E0BC16DCF04AEE6FA500F`

## 2026-07-23 캔버스 확대 기준 재설정

- 수정: 기존 화면상 120% 크기를 새 기준 100%로 맞췄다. 앱 시작값과 하단 확대 초기화 버튼은 100%이며, 확대·축소 시 스케일과 캔버스 하단 여백도 같은 보정 기준을 사용한다.
- 검증: TypeScript와 Vitest 33건을 통과했다. EXE는 사용자 요청 전 재빌드하지 않았다.

## 2026-07-23 레이어 그룹 선택 표시 강화

- 수정: 그룹 만들기 모드에서 각 블록의 맨 왼쪽에 테두리 있는 `선택` 버튼을 표시하고, 선택하면 청록색 체크 상태로 바뀌게 했다.
- 안내: 레이어 상단에 “각 블록 맨 왼쪽의 선택 버튼”을 누르라는 안내를 추가했다.
- 검증: TypeScript와 Vitest 33건을 통과했다. EXE는 사용자 요청 전 재빌드하지 않았다.

## 2026-07-23 이미지 라이브러리 활성 폭 확장

- 수정: 왼쪽 `이미지` 메뉴가 활성화된 동안에만 왼쪽 패널 전체 폭을 기존 292px의 2배인 584px로 확장한다. 레일 폭은 유지하고 이미지 라이브러리 영역을 520px로 넓혔다.
- 사용성: 넓어진 이미지 라이브러리에서는 썸네일을 4열로 표시해 이미지 파일명·URL을 더 빠르게 확인할 수 있다. 다른 왼쪽 메뉴와 모바일 화면은 기존 폭을 유지한다.
- 검증: TypeScript와 Vitest 33건을 통과했다. EXE는 사용자 요청 전 재빌드하지 않았다.

## 2026-07-23 공통 텍스트 띄어쓰기 입력 안정화

- 원인: 공통 텍스트의 리치 에디터가 한글 IME 조합 중에도 매 입력 이벤트마다 저장을 시도해, 단어 사이 공백이 아직 조합 중일 때 이전 마크업이 반영될 여지가 있었다.
- 수정: 조합 중에는 저장을 보류하고 `compositionend`에서 한 번만 반영하도록 바꿨다. 일반 입력과 포커스 이탈 저장은 그대로 유지한다.
- 회귀 검증: 한글 단어 사이 공백과 연속 공백을 리치 텍스트 정리 단계에서 보존하는 테스트를 추가했다. TypeScript와 Vitest 34건을 통과했다. EXE는 사용자 요청 전 재빌드하지 않았다.

## 2026-07-23 미리보기 목록·상세 일정표 캔버스 동기화

- 수정: 전체 미리보기·독립 HTML·디자이너 전달 ZIP의 목록 블록을 캔버스와 같은 번호형 목록 또는 특전형 강조 카드로 출력하도록 별도 렌더러를 적용했다. 고정 높이 박스 안에 단순 `<ul>`로 들어가던 문제도 제거했다.
- 수정: 상세 일정표 미리보기의 오래된 베이지 배경을 제거해 가운데 캔버스와 같은 흰 배경으로 맞췄다.
- 검증: 목록 특전형·상세 일정표 배경 동기화 회귀 테스트를 추가했고 TypeScript와 Vitest 35건을 통과했다. EXE는 사용자 요청 전 재빌드하지 않았다.

## 2026-07-23 EXE 재빌드 시도 — 배포 교체 대기

- 완료: 계약 동기화·검사, TypeScript, Vitest 35건, Vite production build, Electron Windows x64 portable 패키징을 통과했다.
- BLOCKED: `release\\내일스패셜-캔버스-1.0.0-portable.exe`가 실행 중인 기존 포터블 앱(PID 27536)에 잠겨 있어 새 EXE 덮어쓰기와 구버전 삭제를 완료하지 못했다.
- 다음: 실행 중인 포터블 앱을 종료한 뒤 최신 패키지를 `release`로 교체하고 SHA-256을 갱신한다.

## 2026-07-23 EXE 최신본 교체 완료

- 정리: 실행 종료 후 새 포터블 EXE로 교체했으며 `release`에는 최신 `내일스패셜-캔버스-1.0.0-portable.exe` 1개만 남겼다.
- SHA-256: `EBC5F5FFCFBFA7E00A3F26A58520628A6F22BADEFE0A7B1CD62B08126DBDF9D9`
- 확인: EXE 개수 1개와 SHA256SUMS 기록 일치를 확인했다.

## 2026-07-23 JSON 파일 저장 흐름 및 HTML 카드 위치 수정

- 기능: 상단 `파일` 메뉴에 `저장`, `다른 이름으로 저장`, `종료`를 추가했다. 처음 저장은 JSON 위치를 선택하고, 이후 저장은 연결된 JSON 파일에 덮어쓴다. 다른 이름으로 저장은 언제나 새 파일을 선택한다.
- 종료: 종료 확인창도 `저장 후 종료`, `다른 이름으로 저장 후 종료`, `저장하지 않고 종료`, `취소`로 분리했다. 저장 대화상자 취소 시 앱은 닫히지 않는다.
- 보존: 프로젝트별 JSON 파일 경로는 이 PC의 작업공간에만 저장해, 로드용 JSON 계약 자체에는 로컬 경로를 넣지 않는다.
- 버그: 제공된 `푸꾸옥 3박4일 패키지 (아쿠아선 호텔).html`을 확인해 이미지·설명 카드가 캔버스 바깥으로 밀리던 원인을 찾았다. 독립 HTML의 오래된 음수 여백을 캔버스와 같은 내부 18px 여백으로 덮어썼다.
- 검증: TypeScript, Vitest 36건, Electron main/preload JavaScript 문법 검사를 통과했다. EXE는 사용자 요청 전 재빌드하지 않았다.

## 2026-07-23 콘텐츠 그룹 블록 이동 유지

- 원인: 레이어에서 블록 하나를 위·아래로 이동할 때마다 해당 블록을 콘텐츠 그룹에서 먼저 제거하고 있었다. 이 때문에 2개짜리 그룹은 즉시 1개만 남아 그룹 자체가 사라졌다.
- 수정: 그룹에 속한 블록의 이동 요청은 개별 블록이 아니라 콘텐츠 그룹 전체를 한 칸 이동하도록 통합했다. 그룹 이름·접힘 상태·블록 순서는 그대로 유지한다.
- 유지 규칙: 실제로 블록을 삭제한 경우에만 해당 블록을 그룹에서 분리하고, 남은 블록이 2개 미만이면 그룹을 정리한다.
- 검증: 그룹 구성원 이동 후 그룹 유지 회귀 테스트를 추가했고 TypeScript와 Vitest 37건을 통과했다. EXE는 사용자 요청 전 재빌드하지 않았다.

## 2026-07-23 외부 이미지 라이브러리 바로가기 및 EXE 배포

- 기능: 왼쪽 `이미지` 패널 상단에 `라이브러리` 영역을 추가했다. Getty Images, Pixabay, Pexels, Unsplash 버튼을 누르면 각각 기본 브라우저의 해당 이미지 사이트로 열린다.
- 배포: 계약 동기화·검사, TypeScript, Vitest 37건, Vite production build, Electron Windows x64 portable 패키징을 통과했다.
- 정리: `release` 폴더에서 구버전 EXE를 제거하고 최신 `내일스패셜-캔버스-1.0.0-portable.exe` 1개만 남겼다.
- SHA-256: `88D48AAD74538897EC6F412CD1A582C19D6DAD61F44D7206D75CBB76718FB81E`

## 2026-07-23 편집 화면 단순화 및 EXE 재배포

- 정리: 상단 `AI 작성` 버튼, 프로젝트 보드 진입, `프로젝트` 버튼, `새 프로젝트` 버튼을 화면에서 제거했다.
- 파일: 하단 고정 `불러오기` 버튼을 없애고, 상단 `파일` 메뉴의 첫 항목 `불러오기`로 옮겼다. 로드용 JSON과 YAML 파일을 그대로 열 수 있다.
- 이미지: 외부 사이트 버튼을 항상 보이는 박스에서 제거하고, 이미지 패널의 상위 `라이브러리` 접기·펼치기 버튼 아래에 Getty Images, Pixabay, Pexels, Unsplash 바로가기를 배치했다.
- 배포: 계약 동기화·검사, TypeScript, Vitest 37건, Vite production build, Electron Windows x64 portable 패키징을 통과했다. `release`에는 최신 EXE 1개만 유지했다.
- SHA-256: `ABA64C050ADECEDAA9A0601637BEC2A39FA8BE02FD491E74A9154E1E9A1CF7BB`

## 2026-07-23 콘텐츠 그룹 개별 이동 및 파일 메뉴 닫힘

- 그룹 규칙 변경: 레이어의 개별 블록 이동은 콘텐츠 그룹 전체를 건너뛰지 않고 한 칸씩 이동한다. 다른 블록이 그룹 안으로 들어가거나 그룹 구성원이 밖으로 나가 연속 배치가 깨지면, 해당 그룹은 즉시 해제된다.
- 복제: 콘텐츠 그룹 구성원을 복제할 때도 곧바로 그룹 유효성을 다시 계산해, 다음에 파일을 다시 열었을 때 뒤늦게 그룹이 사라지는 문제를 제거했다.
- 파일 메뉴: 바깥 영역·왼쪽 탭을 누르면 열린 파일 메뉴가 즉시 닫히도록 처리했다.
- 검증: 그룹 연속성 해제 회귀 테스트, TypeScript, Vitest 37건 통과. 실제 화면에서 파일 메뉴를 열고 이미지 탭을 선택했을 때 메뉴가 닫히는 것을 확인했다. EXE는 사용자 요청 전 재빌드하지 않았다.

## 2026-07-23 EXE 재빌드 및 배포본 정리

- 완료: 계약 동기화·검사, TypeScript, Vitest 37건, Vite production build, Electron Windows x64 portable 패키징을 통과했다.
- 정리: `release` 폴더에 최신 `내일스패셜-캔버스-1.0.0-portable.exe` 1개만 유지했다.
- SHA-256: `888CDCDBA83B2B8F82BB45486E88FC479E2DEC3540647C77E6C3856FEC84385D`
- 배포 메모: 현재 포터블 EXE는 자동 업데이트 대상이 아니므로, 자동 업데이트 배포 전환 시 NSIS 설치형 + HTTPS 배포 위치 + 업데이트 메타데이터 구조가 필요하다.

## 2026-07-23 GitHub Releases 자동 업데이트 전환

- 배포: 공개 GitHub 저장소 `KAKNAKIAK/naeil-special-canvas`를 생성하고, 앱 버전을 `1.0.1`로 올렸다.
- 업데이트: `electron-updater`를 적용해 설치형 앱이 실행될 때 GitHub Releases의 새 버전을 확인·다운로드하고, 완료 시 즉시 재시작 또는 다음 종료 시 설치를 선택하게 했다.
- 패키징: 포터블 대상은 제거하고 NSIS 설치형으로 전환했다. `build-exe.bat`은 임시 배포 산출물을 먼저 비우고 설치 파일·blockmap·`latest.yml`만 `release`에 남긴다.
- 게시: `publish-github-release.bat`은 현재 GitHub CLI 로그인 토큰을 빌드 프로세스에만 전달해 GitHub Release에 세 자산을 게시한다.
- 검증: 계약 동기화·검사, TypeScript, Vitest 37건, Vite production build, Electron Windows x64 NSIS 패키징을 통과했다. `latest.yml`의 설치 파일명·blockmap 존재·SHA-256 일치를 확인했다.

## 2026-07-23 설치본 작성 스킬 동봉

- 구성: `G:\내 드라이브\안티그래비티\.agents\skills\naeil-special-canvas-writer` 폴더 전체를 빌드 공간으로 복사하고, NSIS 설치본의 `resources\skills\naeil-special-canvas-writer`에 동봉하도록 했다.
- 버전: 자동 업데이트 배포용 설치본 버전을 `1.0.2`로 올렸다.
- 안전: 빌드 전 작성 스킬의 `SKILL.md` 존재를 검사해, 원본 폴더가 없을 때는 불완전한 설치본을 만들지 않고 중단한다.
- 검증: NSIS 패키징 후 `resources\skills\naeil-special-canvas-writer`에서 핵심 파일 5종과 전체 16개 파일을 확인했다. 설치 파일·blockmap·`latest.yml`도 같은 `1.0.2` 버전으로 생성됐다.

## 2026-07-23 이미지 카드 삭제 복구 버그 수정

- 원인: 이미지 프리셋의 기본 이미지 카드(`media`)를 삭제해도 레이아웃 정규화가 기본 카드를 매번 다시 만들어, 삭제 토스트만 보이고 화면에는 그대로 남았다.
- 수정: 기본 텍스트·이미지 카드의 삭제 상태를 `removedLayoutBoxIds`에 보존하도록 했다. 따라서 이미지 카드는 캔버스와 저장·재로드 결과에서 실제로 사라지며, 원본 이미지 자산은 라이브러리에 유지된다.
- 검증: 기본 이미지 카드 삭제 후 정규화·프로젝트 재로드에서도 복구되지 않는 회귀 테스트를 추가했다. 계약 검사, TypeScript, Vitest 38건, Vite production build를 통과했다.

## 2026-07-23 v1.0.3 배포 및 GitHub Release 게시

- 수정: 이미지 프리셋의 기본 이미지 카드 삭제 복구 버그 수정 반영.
- 버전: 앱 버전을 `1.0.3`으로 상향 변경.
- 빌드: 계약 검사, TypeScript, Vitest 38건, Vite production build, Electron NSIS 패키징 수행 (`naeil-special-canvas-writer` 스킬 동봉).
- 배포: `release` 폴더 구버전 정리 후 Setup EXE, blockmap, `latest.yml` 검증, main push 및 GitHub Release `v1.0.3` 게시.

## 2026-07-23 직접 편집 이미지 배치 HTML 내보내기 일치

- 원인: 이미지 프리셋의 `직접 편집`은 캔버스에서 12칸 그리드의 이미지별 열·행·크롭 위치를 사용하지만, 독립 HTML은 이를 읽지 않고 이미지 수만 기준으로 1열/2열 그리드를 다시 만들고 있었다.
- 수정: 독립 HTML도 `mediaLayoutItems`의 열·행·가로/세로 칸 수·크롭 위치를 적용하도록 변경했다. 캔버스의 이미지 플로우와 동일하게 12칸, 행당 108px, 간격 5px 기준으로 출력한다.
- 검증: 직접 배치한 7:5 상하 분할을 HTML에서 같은 grid-column/grid-row/object-position 값으로 출력하는 회귀 테스트를 추가했다. 계약 검사, TypeScript, Vitest 39건, Vite production build를 통과했다.
- 배포: 사용자 요청 전 EXE 및 GitHub Release는 생성하지 않았다.

## 2026-07-23 콘텐츠 그룹 경계 이동 수정

- 원인: 레이어의 개별 블록 이동이 바로 옆 블록과 단순 교환되어, 옆 블록이 콘텐츠 그룹에 속한 경우 그룹 중간으로 삽입되고 그룹이 해제됐다.
- 수정: 레이어 이동 단위를 `일반 블록 또는 콘텐츠 그룹 전체`로 통일했다. 일반 블록은 그룹의 앞·뒤를 한 번에 통과하고, 그룹 구성원을 이동해도 그룹 전체가 이동한다.
- 검증: 일반 블록의 위·아래 그룹 통과와 그룹 구성원 이동 모두 그룹 연속성이 유지되는 회귀 테스트를 추가했다. 계약 검사, TypeScript, Vitest 39건, Vite production build를 통과했다.

## 2026-07-23 v1.0.4 배포 및 GitHub Release 게시

- 수정: 직접 편집 이미지 배치의 독립 HTML 내보내기 일치 및 콘텐츠 그룹 경계 이동 그룹 유지 수정 반영.
- 버전: 앱 버전을 `1.0.4`로 상향 변경.
- 빌드: 계약 검사, TypeScript, Vitest 39건, Vite production build, Electron NSIS 패키징 수행 (`naeil-special-canvas-writer` 스킬 동봉).
- 배포: `release` 폴더 구버전 정리 후 Setup EXE, blockmap, `latest.yml` 검증, main push 및 GitHub Release `v1.0.4` 게시.

## 2026-07-23 AI 자동작성 콘텐츠 기반 구성 다양화

- 원인: draft brief의 블록 순서가 상품 카테고리별 고정 배열이라, 서로 다른 자료도 비슷한 텍스트·이미지·목록 순서로 생성됐다.
- 수정: 앱과 writer Skill이 일정 줄·시간 표기·이미지 수·시각 소개·특전·확정 정보 신호를 분석해 사진 중심, 상세 일정, 특전·장점, 간략 일정, 스토리형, 최소 구성 중 하나를 선택하도록 통일했다.
- 기록: draft brief와 승인된 Project JSON의 `approvedBrief`에 `composition.strategy`와 `composition.reason`을 함께 남겨 생성 근거를 확인할 수 있게 했다. 사용자는 `compositionHint`로 방향을 지정할 수 있다.
- 문구: 블록 제목과 본문도 선택 전략에 따라 달라지며, 확인된 사실 중심의 짧고 담백한 문구 규칙을 유지한다.
- 검증: 계약 동기화·검사, TypeScript, Vitest 41건, Vite production build, Python 문법 검사, 4개 상이한 입력 전략 출력, writer Skill 실생성, 브라우저 콘솔 오류 없음까지 확인했다.
- 배포: 사용자 요청에 따라 EXE 빌드·배포는 수행하지 않았다.

## 2026-07-24 앱 전용 작성 스킬 자동 동기화

- 경로: 설치본 실행 시 동봉된 `naeil-special-canvas-writer`를 `%APPDATA%\NaeilSpecialCanvas\skills\naeil-special-canvas-writer`에 동기화한다.
- 범위: 앱 전용 사용자 데이터만 교체한다. 사용자의 공용 `.agents`, `.codex` 스킬 폴더와 그 안의 파일은 수정하지 않는다.
- 업데이트: 새 앱 버전이 설치·자동 업데이트된 뒤 최초 실행 시 최신 동봉 스킬로 원자적으로 교체하고, `.installed-by.json`에 앱 버전과 동기화 시각을 기록한다.
- 검증: Electron main 문법 검사, 계약 검사, TypeScript, Vitest 41건, Vite production build를 통과했다. EXE 및 GitHub Release는 사용자 요청 전 생성하지 않았다.

## 2026-07-24 구분자 기반 항목 공백 입력 보존

- 원인: 상세 일정표와 이미지·설명 카드의 항목은 `|` 구분 문자열로 보관한다. 기존 분해 함수가 모든 부분을 `trim()`해, 설명·제목 끝에 막 입력한 공백이 제어 입력의 재렌더링 때 사라졌다.
- 수정: 두 프리셋의 분해·결합을 `preset-item-format.ts` 공통 모듈로 통합했다. 구분자가 만든 공백 한 칸만 제거하고 사용자가 입력한 앞·뒤·반복 공백은 그대로 보존한다. 오른쪽 패널과 가운데 인라인 편집 모두 같은 모듈을 사용한다.
- 점검: 일반 텍스트, 목록, 간략 일정표, 아이콘 카드, 캡션은 문자열을 직접 저장해 이 문제가 없었다. AI 초안 생성부의 구분자 조합은 사용자 입력 중 재렌더링되는 경로가 아니어서 수정 대상이 아니다.
- 검증: 이미지·설명 카드와 상세 일정표의 공백 보존 회귀 테스트를 추가했고 계약 검사, TypeScript, Vitest 43건, Vite production build를 통과했다.

## 2026-07-24 목록 프리셋 독립 HTML 줄바꿈 일치

- 원인: 앱의 목록 항목은 `white-space: pre-wrap`으로 제목·본문 사이에 넣은 줄바꿈을 보존하지만, 독립 HTML의 목록 항목 `<span>`에는 같은 규칙이 없어 줄바꿈이 공백처럼 합쳐졌다.
- 수정: 독립 HTML의 목록 항목에도 `white-space: pre-wrap`을 적용해 앱과 같이 제목과 본문 줄 구성을 유지한다. 스타일은 HTML `<head>` 안에 포함해 빈 문서·디자이너 ZIP의 HTML 구조도 유지했다.
- 검증: 특전형 목록의 줄바꿈 보존 회귀 검사를 추가했고 계약 검사, TypeScript, Vitest 43건, Vite production build를 통과했다.

## 2026-07-24 첫 안내 종료 후 블록 탭 복귀

- 원인: 첫 안내의 마지막 단계가 레이어 탭을 활성화한 뒤 안내만 닫아, 사용자가 처음 보는 화면이 레이어 패널에 머물렀다.
- 수정: 완료·건너뛰기·다시 보지 않기 모두 안내 종료 시 단계 상태를 초기화하고 왼쪽 패널을 `블록` 탭으로 되돌린다.
- 검증: 계약 검사, TypeScript, Vitest 43건, Vite production build를 통과했다.

## 2026-07-24 v1.0.5 배포 및 GitHub Release 게시

- 수정: AI 자동작성 콘텐츠 기반 구성 다양화 및 앱 전용 작성 스킬 자동 동기화 기능 반영.
- 버전: 앱 버전을 `1.0.5`로 상향 변경.
- 빌드: 계약 검사, TypeScript, Vitest 41건, Vite production build, Electron NSIS 패키징 수행 (`naeil-special-canvas-writer` 스킬 동봉).
- 배포: `release` 폴더 구버전 정리 후 Setup EXE, blockmap, `latest.yml` 검증, main push 및 GitHub Release `v1.0.5` 게시.

## 2026-07-24 AI 작성 Skill 사용자용 블록명 통일

- 원인: writer Skill이 기획안 대화에서도 `image`, `icon-card`, `menu-zigzag`, `timeline` 등 내부 블록 ID를 그대로 보여 줘 앱 화면의 한국어 블록명과 맞지 않았다.
- 수정: 사용자 제안·질문·최종 답변에서는 `이미지`, `아이콘 카드`, `이미지·설명 카드`, `상세 일정표` 등 캔버스 표시명만 쓰고, 내부 ID·JSON 필드명은 기술 요청 또는 생성 파일 안에서만 쓰도록 분리했다.
- 구성 전략도 사진 중심·상세 일정 중심·특전·장점 중심 등 사용자용 명칭으로 안내하도록 기준과 라우팅 표를 갱신했다.
- 검증: Skill 구조 검증을 수행했다. 사용자 요청 전 EXE 빌드·배포는 하지 않았다.

## 2026-07-24 AI 작성 Skill 버전 추적 추가

- 추가: `naeil-special-canvas-writer/skill-manifest.json`에 Skill 이름·버전·변경 요약을 관리하도록 했고, 이번 사용자용 블록명 통일 변경은 `1.0.1`로 기록했다.
- 연동: 로드 JSON 생성 시 하드코딩된 생성기 버전 대신 manifest의 `version`을 `generator.version`에 기록하도록 수정했다.
- 검증: manifest 기반 로드 JSON 생성과 Skill 구조 검증을 수행했다. 사용자 요청 전 EXE 빌드·배포는 하지 않았다.

## 2026-07-24 AI 작성 Skill 산출물 최소화 및 로드 JSON 경로 고정

- 변경: 승인 기획안은 기본적으로 표준 출력·입력으로만 전달하고, 별도 파일로 저장하지 않도록 `build_draft_brief.py`와 `build_project.py`에 표준 입출력 경로를 추가했다.
- 변경: 검수보고서는 오류 또는 사용자 요청이 있을 때만 파일로 만든다. 기본 검수는 콘솔 결과만 확인한다.
- 응답: 로드 JSON 생성 후 최종 답변에 `프로젝트 로드 JSON: [파일명](절대 경로)` 행을 반드시 넣고, 미생성 시에도 사유를 명시하도록 Skill 규칙을 강화했다.
- 버전: writer Skill `1.0.4`.
- 보완: 상품 자료와 내부 draft도 표준 입력으로 받을 수 있고, 사용자와 방향을 합의한 뒤에만 `--approve-draft`로 실행 중 draft를 승인 처리해 중간 JSON 파일 없이 최종 로드 JSON만 남긴다.
- 보완: Windows 표준 입력의 BOM을 처리하고, 최종 답변에는 로드 JSON 경로 바로 아래에 메인 앱에서 불러오면 캔버스 초안이 표시된다는 안내를 넣는다.
- 검증: 표준 입력 기반 Project JSON 생성, Skill 구조·Python 문법 검사를 수행했다. 사용자 요청 전 EXE 빌드·배포는 하지 않았다.

## 2026-07-24 v1.0.6 배포 및 writer Skill 1.0.4 동봉

- 수정: 사용자용 블록명 통일, Skill 버전 이력, 중간 승인 기획안·검수보고서 파일 최소화, 로드 JSON 경로와 앱 불러오기 안내를 반영한 writer Skill `1.0.4`를 동봉했다.
- 버전: 앱 `1.0.6`, writer Skill `1.0.4`.
- 빌드: 계약 검사, TypeScript, Vitest 41건, Vite production build, Electron Windows x64 NSIS 패키징을 통과했다.
- 검증: 설치본의 `resources\skills\naeil-special-canvas-writer\skill-manifest.json`과 앱 전용 동기화 폴더에서 Skill `1.0.4`, 앱 `1.0.6`을 확인했다. Setup EXE SHA-256은 `F7A9D66ED603C076013EFAE5E07C21B2FDB8D20866855F3A6CF30105F73A9A90`이며, `latest.yml` SHA-512·blockmap·SHA256SUMS와 원격 Release 자산 4종이 일치했다.
- 배포: GitHub Release [v1.0.6](https://github.com/KAKNAKIAK/naeil-special-canvas/releases/tag/v1.0.6) 게시 완료.

## 2026-07-24 writer Skill humanize-korean 문구 검수 연동

- 구조: writer Skill이 사용자 제안·최종 안내와 캔버스 제목·본문·카드·목록 문구를 확정하기 전에 동봉된 `references/humanize-korean/SKILL.md`의 빠른 검수 기준을 적용하도록 연결했다.
- 범위: 번역투·기계적 반복·과한 연결어·광고성 표현만 다듬고, 사실·수치·날짜·고유명사·상품명·인용·확인 필요 상태·이미지 파일명과 URL은 보존한다.
- 출력: 검수 결과 파일·보고서는 기본 생성하지 않고, 자연화한 문구만 draft와 사용자 답변에 반영한다.
- 동봉: `humanize-korean` 원본 Skill·레퍼런스·스크립트·테스트 30개 파일(약 350KB)을 writer Skill 내부에 포함해, 설치된 앱이나 다른 PC에서도 외부 `.agent` 경로 없이 검수할 수 있다.
- 버전: writer Skill `1.0.6`.

## 2026-07-24 v1.0.7 배포 및 GitHub Release 게시

- 수정: humanize-korean Skill·레퍼런스·스크립트·테스트 전체를 writer Skill 내부에 동봉하여 외부 `.agent` 경로 없는 환경에서도 캔버스 문구 검수 동작 보장.
- 버전: 앱 `1.0.7`, writer Skill `1.0.6`.
- 빌드: 계약 동기화·검사, TypeScript, Vitest 41건, Vite production build, Electron Windows x64 NSIS 패키징 통과.
- 검증: 설치본의 `resources\skills\naeil-special-canvas-writer\skill-manifest.json`(`1.0.6`) 및 `references\humanize-korean\SKILL.md` 포함 확인. 앱 실행 시 `%APPDATA%\NaeilSpecialCanvas\skills\naeil-special-canvas-writer` 동기화 정상 동작 확인.
- 해시: Setup EXE SHA-256 `963E8C84FBFC37603E90D17F40C59BA2E3DA77C63668337896318DE8B0097FD1`, `latest.yml` SHA-512·blockmap·SHA256SUMS와 원격 Release 자산 4종 일치.
- 배포: GitHub Release [v1.0.7](https://github.com/KAKNAKIAK/naeil-special-canvas/releases/tag/v1.0.7) 게시 완료.

## 2026-07-24 첫 사용 흐름 및 도움말 개선 (미배포)

- 범위: 첫 화면 시작 방식 3개, 최초 사용 4단계 안내, 작업형 도움말 패널까지만 반영했다. EXE 빌드와 GitHub Release는 사용자 지시 전 수행하지 않았다.
- 시작: 빈 기획안은 블록 선택으로 바로 이어지고, 샘플 기획안은 실제로 편집 가능한 괌 가족 휴양 예시와 연습용 이미지를 새 프로젝트로 연다. 기존 작업본은 덮어쓰지 않는다.
- 안내: 블록 추가 → 문구 더블클릭 → 이미지 연결 → 미리보기·내보내기의 4단계 안내를 제공하며, 건너뛰기·다시 보지 않기·도움말에서 재실행을 지원한다.
- 도움말: 왼쪽 도움말 버튼을 우측 작업형 패널로 바꾸고, 블록·문구·이미지·상세 일정·ZIP·로드 JSON 흐름과 `naeil-special-canvas-writer`를 통한 JSON 생성/불러오기 안내를 넣었다. 상단과 도움말에 앱 버전도 표시한다. 런타임은 프로젝트 루트의 `package.json`을 복사하지 않으므로, `src/app-version.ts`를 앱 버전의 단일 표시값으로 둔다.
- 검증: `git diff --check`를 통과했다. 다만 현재 작업 폴더에 `node_modules`가 없어 TypeScript/Vite 빌드는 `lucide-react`, `yaml`, `vitest` 등 기존 의존성을 찾지 못해 중단됐다. 의존성 설치는 프로젝트 규칙상 사용자 승인 없이 수행하지 않았다.

## 2026-07-24 화면 대입형 첫 사용 안내 (미배포)

- 변경: 중앙 모달로만 설명하던 4단계 안내를 화면 위 가이드로 교체했다. 블록 패널 → 캔버스 문구 영역 → 이미지 박스/이미지 탭 → 미리보기·내보내기 순서로 실제 위치를 밝은 테두리와 말풍선으로 가리킨다.
- 동작: 안내 단계에 따라 필요한 왼쪽 탭을 열고, 화면 크기·스크롤 변화에도 하이라이트 위치를 다시 계산한다. 기존 `도움말 > 처음 안내 다시 보기`, 건너뛰기, 다시 보지 않기 흐름은 유지했다.
- 검증: 계약 검사와 `git diff --check`를 통과했다. 실행 중인 로컬 서버에서 2단계 캔버스, 3단계 이미지 탭, 4단계 내보내기 위치가 각각 선택되는 것을 확인했다.
- 배포: EXE 빌드와 Release는 사용자 요청 전 수행하지 않았다.
- 보완: 이미지·레이어의 역할 설명을 분리해 5단계로 확장했다. 이미지는 파일·URL 자산을 여러 블록에서 재사용하는 흐름, 레이어는 블록 순서·그룹을 정리하는 흐름을 실제 왼쪽 탭 위치에서 안내한다. 이미지 탭을 감싸던 가이드용 요소는 제거해 기존 레일 버튼의 폰트 크기·높이 규격을 복구했다.

## 2026-07-24 이미지 입력 안내 및 캔버스 공백 입력 보완 (미배포)

- 안내: 이미지 단계와 도움말에 내 PC 이미지 파일 업로드와 이미지 URL 붙여 넣기 방식을 함께 명시했다.
- 수정: 인라인 텍스트 편집을 시작할 때 전체 문장을 자동 선택하던 동작을 제거해, 새 항목의 기본 문구가 공백 입력 한 번으로 통째로 비어 보이지 않게 했다. 입력 중인 `input`·`textarea`·리치 텍스트는 키 이벤트를 카드·캔버스로 전파하지 않아 스페이스를 포함한 문자가 편집 필드 안에서 처리된다.
- 검증: 로컬 런타임 TypeScript 검사와 Vitest 41건을 통과했다. EXE 빌드와 Release는 사용자 요청 전 수행하지 않았다.

## 2026-07-24 앱 UI 문구 자연화 검수 (진행 중)

- 기준: `im-not-ai/.claude/skills/humanize-korean` v1.3.1의 의미 보존·국소 수정 원칙을 적용한다.
- 범위: 기능명·블록명·파일 형식·JSON 용어는 유지하고, 블록 설명·도움말·이미지 연결 안내·AI 초안 기본 문구처럼 읽히는 문장만 다듬는다.
- 1차 수정: 번역투와 절차형 문장을 줄여 텍스트·목록·간략 일정표·이미지·설명 카드의 설명과 AI 초안 기본 문구를 짧고 직접적으로 정리했다.

## 2026-07-24 이미지 안내 위치 및 인라인 편집 폭 보정 (미배포)

- 안내: 첫 사용 안내의 이미지 단계가 왼쪽 이미지 패널 전체를 실제로 비추며, 이미지 불러오기와 URL 입력 영역을 보면서 “이미지 파일을 올리거나 이미지 URL을 붙여 넣으세요”라는 안내를 읽도록 수정했다.
- 수정: 목록 항목의 캔버스 인라인 편집 필드가 flex 행 안에서 내용 폭만 차지해 쪼그라들지 않도록, 남은 행 폭을 안정적으로 사용하게 보정했다.
- 검증: 계약 검사, `git diff --check`, 로컬 런타임 TypeScript 검사와 Vitest 41건을 통과했다. EXE 빌드와 Release는 사용자 요청 전 수행하지 않았다.

## 2026-07-24 상세 일정표 일차 경계 보정 (미배포)

- 원인: 상세 일정표의 일차 구분은 일정 항목의 인덱스로 저장된다. 개별 일정 항목을 삭제할 때 뒤 일차의 시작 인덱스를 함께 줄이지 않아, 2일차 라벨이 사라지고 해당 일정이 1일차처럼 이어져 보였다.
- 수정: 일정 삭제 시 뒤 일차 시작점을 자동으로 한 칸 당기고, 삭제한 일정이 그 일차의 유일한 항목이면 빈 일차 구분도 함께 제거한다. 일정 복제 시에도 뒤 일차 시작점을 밀어 경계가 유지되게 했다.
- 검증: 1일차 두 번째 일정 삭제 후 `[0, 2] → [0, 1]`로 2일차 라벨이 유지되는 단위 검사를 추가했다. 계약 검사, `git diff --check`, 로컬 런타임 TypeScript 검사와 Vitest 42건을 통과했다. EXE 빌드와 Release는 사용자 요청 전 수행하지 않았다.

## 2026-07-24 작업 도움말 정리 및 첫 안내 문구 검수 (미배포)

- 제거: 작업 도움말 상단의 `AI 스킬 사용법` 카드와 스킬 사용 절차를 삭제했다. 로드 JSON 항목은 파일을 불러와 캔버스 초안을 여는 기능 안내만 남겼다.
- 문구: `im-not-ai/.claude/skills/humanize-korean` v1.3.1의 의미 보존·국소 수정 기준으로 첫 안내 제목을 행동형 문장으로 정리했다. 예: `블록으로 페이지 흐름을 만드세요` → `블록을 추가해 보세요`.
- 검증: 계약 검사, `git diff --check`, 로컬 런타임 TypeScript 검사와 Vitest 42건을 통과했다. EXE 빌드와 Release는 사용자 요청 전 수행하지 않았다.

## 2026-07-24 CI 새 빈 캔버스 진입 (미배포)

- 동작: 상단 내일투어 CI를 키보드로도 누를 수 있는 버튼으로 변경했다. 클릭하면 현재 작업이 자동 저장되어 프로젝트에서 다시 열 수 있음을 알리는 확인창을 먼저 띄우고, 확인 시 새 빈 캔버스를 연다.
- 상태: 새 프로젝트를 열 때 선택된 블록·이미지·항목 상태와 편집 이력을 함께 초기화하고 블록 탭을 표시한다.
- 검증: 계약 검사, `git diff --check`, 로컬 런타임 TypeScript 검사와 Vitest 42건을 통과했다. EXE 빌드와 Release는 사용자 요청 전 수행하지 않았다.

## 2026-07-24 v1.0.8 배포 및 GitHub Release 게시

- 수정: 첫 사용 안내(5단계 가이드), 작업 도움말 정리, CI 클릭 시 새 빈 캔버스 시작, 상세 일정표 일차 경계 보정, 캔버스 공백 입력 보정 반영.
- 버전: 앱 `1.0.8`, writer Skill `1.0.6`.
- 빌드: 계약 동기화·검사, TypeScript, Vitest 42건, Vite production build, Electron Windows x64 NSIS 패키징 통과.
- 검증: 설치본의 `resources\skills\naeil-special-canvas-writer\SKILL.md` 동봉 확인.
- 해시: Setup EXE SHA-256 `270B583E8CC99B1B847CAE304EA528342ACDCC0EC93E5CCB7547E55FDDFDFE31`, `latest.yml` SHA-512·blockmap·SHA256SUMS 일치.
- 배포: GitHub Release [v1.0.8](https://github.com/KAKNAKIAK/naeil-special-canvas/releases/tag/v1.0.8) 게시 완료.

## 2026-07-24 v1.0.9 배포 및 GitHub Release 게시

- 수정: 상세 일정표 및 이미지·설명 카드의 구분자 기반 항목 공백 입력 보존 반영.
- 버전: 앱 `1.0.9`, writer Skill `1.0.6`.
- 빌드: 계약 동기화·검사, TypeScript, Vitest 43건, Vite production build, Electron Windows x64 NSIS 패키징 통과.
- 검증: 설치본의 `resources\skills\naeil-special-canvas-writer\SKILL.md` 동봉 확인.
- 해시: Setup EXE SHA-256 `11F59124310D5CEA3CD213A587746C80AE05E94F8E2569BC43DBD97F0FFE2B22`, `latest.yml` SHA-512·blockmap·SHA256SUMS 일치.
- 배포: GitHub Release [v1.0.9](https://github.com/KAKNAKIAK/naeil-special-canvas/releases/tag/v1.0.9) 게시 완료.

## 2026-07-24 v1.0.10 배포 및 GitHub Release 게시

- 수정: 목록 프리셋 독립 HTML 항목의 줄바꿈(`white-space: pre-wrap`) 보존 반영.
- 버전: 앱 `1.0.10`, writer Skill `1.0.6`.
- 빌드: 계약 동기화·검사, TypeScript, Vitest 43건, Vite production build, Electron Windows x64 NSIS 패키징 통과.
- 검증: 설치본의 `resources\skills\naeil-special-canvas-writer\SKILL.md` 동봉 확인.
- 해시: Setup EXE SHA-256 `37BA69EF0E92FC3DA8D1C39BEE3F12AB29A5EC3D6B97B44BC6EE471BDC591665`, `latest.yml` SHA-512·blockmap·SHA256SUMS 일치.
- 배포: GitHub Release [v1.0.10](https://github.com/KAKNAKIAK/naeil-special-canvas/releases/tag/v1.0.10) 게시 완료.

## 2026-07-24 v1.0.11 배포 및 GitHub Release 게시

- 수정: 첫 안내 종료 후 왼쪽 패널 `블록` 탭 자동 복귀 반영.
- 버전: 앱 `1.0.11`, writer Skill `1.0.6`.
- 빌드: 계약 동기화·검사, TypeScript, Vitest 43건, Vite production build, Electron Windows x64 NSIS 패키징 통과.
- 검증: 설치본의 `resources\skills\naeil-special-canvas-writer\SKILL.md` 동봉 확인.
- 해시: Setup EXE SHA-256 `38DC189AD747BE9014CD37A5AA381BEC8C87825E309B7B38CCBC313E3CD75BC9`, `latest.yml` SHA-512·blockmap·SHA256SUMS 일치.
- 배포: GitHub Release [v1.0.11](https://github.com/KAKNAKIAK/naeil-special-canvas/releases/tag/v1.0.11) 게시 완료.

## 2026-07-24 v1.0.12 배포 및 GitHub Release 게시

- 수정: 선택한 아이콘 카드의 우측 편집 패널에 `카드 삭제` 버튼 추가 반영.
- 버전: 앱 `1.0.12`, writer Skill `1.0.6`.
- 빌드: 계약 동기화·검사, TypeScript, Vitest 43건, Vite production build, Electron Windows x64 NSIS 패키징 통과.
- 배포: GitHub Release [v1.0.12](https://github.com/KAKNAKIAK/naeil-special-canvas/releases/tag/v1.0.12) 게시 완료.

## 2026-07-24 v1.0.13 배포 및 GitHub Release 게시

- 수정: 여행 아이콘 추가(음식, 마사지, 스파, 수영장, 피트니스, 쇼핑, 가족·키즈, 골프, 온천·목욕) 및 캔버스 아이콘 카드 드래그 앤 드롭 정렬(`⋮⋮` 핸들) 반영.
- 버전: 앱 `1.0.13`, writer Skill `1.0.6`.
- 빌드: 계약 동기화·검사, TypeScript, Vitest 43건, Vite production build, Electron Windows x64 NSIS 패키징 통과.
- 배포: GitHub Release [v1.0.13](https://github.com/KAKNAKIAK/naeil-special-canvas/releases/tag/v1.0.13) 게시 완료.

## 2026-07-24 v1.0.14 배포 및 GitHub Release 게시

- 수정: 독립 HTML 내보내기 시 아이콘 카드 및 간략 일정표의 고정 높이 캔버스 컨테이너 제거 및 자연 높이 렌더링 반영(블록 내부 세로 스크롤 방지).
- 버전: 앱 `1.0.14`, writer Skill `1.0.6`.
- 빌드: 계약 동기화·검사, TypeScript, Vitest 43건, Vite production build, Electron Windows x64 NSIS 패키징 통과.
- 배포: GitHub Release [v1.0.14](https://github.com/KAKNAKIAK/naeil-special-canvas/releases/tag/v1.0.14) 게시 완료.

## 2026-07-24 v1.0.15 배포 및 GitHub Release 게시

- 수정: 독립 HTML 내보내기 시 이미지·설명 카드 및 상세 일정표 공통 제목 규격(`31px / 600 / 1.35`) 및 `Noto Sans KR / Noto Serif KR` 웹폰트 선언 일치(캔버스-독립 HTML 간 제목 줄바꿈 일치).
- 버전: 앱 `1.0.15`, writer Skill `1.0.6`.
- 빌드: 계약 동기화·검사, TypeScript, Vitest 43건, Vite production build, Electron Windows x64 NSIS 패키징 통과.
- 배포: GitHub Release [v1.0.15](https://github.com/KAKNAKIAK/naeil-special-canvas/releases/tag/v1.0.15) 게시 완료.

## 2026-07-27 v1.0.16 배포 및 GitHub Release 게시

- 구성: `2행 이미지` 프리셋(좌우 각 1장 이미지 + 제목 + 설명 2장 구조) 반영, 캔버스-독립 HTML 내보내기 정렬 및 스타일 개선.
- 버전: 앱 `1.0.16`, writer Skill `1.0.6`.
- 빌드: 계약 동기화·검사, TypeScript, Vitest 43건, Vite production build, Electron Windows x64 NSIS 패키징 통과.
- 배포: GitHub Release [v1.0.16](https://github.com/KAKNAKIAK/naeil-special-canvas/releases/tag/v1.0.16) 게시 완료.
